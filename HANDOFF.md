# Tally PWA — 次チャットへの引き継ぎ (現行 v4.43)

**【次チャットの始め方】** このHANDOFF.mdを新チャットに添付して「Tally開発の続き」と伝えれば全文脈が復元される。作業ファイルは/home/claude/pwa/index.html(消失時は/mnt/user-data/outputs/index.htmlから、それも無ければユーザーにリポジトリの現物添付を依頼)。開発フロー規約(pythonヒアドキュメント編集→node構文check→バージョン2箇所バンプ→outputsへcp→present_files必須)とアンカー教訓は本書後半に記載。デプロイはユーザー側のDeploy Tallyショートカット(index.html専用・ファイル受け取りのみ)。HANDOFF.mdは**Deploy HANDOFFショートカット**(共有シート→Working Copyネイティブアクションで write→commit(message:handoff)→push を原子実行——未コミット衝突ダイアログ問題を根治、2026-07-24実弾テスト合格・運用開始)。

> リポジトリでは固定名 `HANDOFF.md` で上書き運用(バージョンはこの見出し、履歴はGit)。

## 0. 現在のフェーズと優先事項
**緊急タスクなし。開発は「データ収集フェーズ」に入った。**機能追加は原則凍結(機能予算: 「月1回以上使うか/入力摩擦を減らすか/意思決定を変えるか」を通らないものは足さない)。

- **分析ダッシュボード(時給・分散・月次トレンド)は閾値到達までビルドしない**: 麻雀30セット/スロット50セッション到達がトリガー。v2.48で実働時刻(startAt/endAt)の記録を開始済みなので、到達時点のデータは時給計算に使える。深夜跨ぎは end<start なら+24hで解釈する規約。
- 投資機能: ユーザーが投資を開始してから(目安: 2027年新年)。器の先行実装は不要とユーザー明言。
- このHANDOFFと RESTORE_RUNBOOK.md はリポジトリに置いてコードと一緒にバージョン管理する方針。
- **作業開始時は必ずユーザーに最新index.htmlをアップロードしてもらうか、リポジトリ(sai2943.github.io/Tally)を基準にする。**

## 1. アプリ概要
- **名前**: Tally(旧: 収支台帳)。**単一HTMLファイルのオフラインPWA**(`index.html`+`sw.js`+`manifest.webmanifest`+アイコンpng)。
- **用途**: 収支管理。家計簿+パチスロ+麻雀+(将来)投資。
- **ユーザー**: 清秀/Fujimura Seishu。EV志向のBPO管理職(仙台)。**忌憚のない意見を強く好む**(迎合を嫌う)。iPhone 17 / iOS 26。
- **GitHub**: github.com/sai2943/Tally / デプロイ: https://sai2943.github.io/Tally/
- **モデル運用**: 2026-07-13からFableは従量課金(usage credits)のためOpus常用。Fableは設計判断・移行・レビュー等の複雑案件のみ。

## 2. デプロイ & 開発フロー(厳守)
- **[DEPLOY]** iOS: Working Copyでファイルを上書き→Commit→Push(ユーザー手動。「いつものpushフローで」と案内すればOK)。更新の反映はアプリをスワイプで閉じて開き直す→更新バー→再読み込み。
- **[DEV] 変更ごとに必ず**:
  1. `str_replace`等で編集
  2. インラインJSを抽出して `node --check`(下記スニペット)
  3. grepで変更箇所検証、可能ならロジックを`node -e`/`python3 -c`でテスト
  4. **バージョンを2箇所バンプ**: コメント `Tally X.XX (localStorage保存・完全オフライン)`(~200行)と `const APP_VERSION = "X.XX"`(~680行)
  5. outputsにコピー→**必ずpresent_filesを呼ぶ**(呼び忘れ厳禁)
- 構文チェック:
```bash
cd /home/claude/pwa && python3 -c "
import re
html=open('index.html',encoding='utf-8').read()
blocks=re.findall(r'<script(?![^>]*\bsrc=)[^>]*>(.*?)</script>',html,re.S)
open('_all.js','w',encoding='utf-8').write('\n;\n'.join(blocks))
" && node --check _all.js && echo SYNTAX_OK && rm -f _all.js
```
- 注意: `uid()`は数値を返すためID比較は`String(x.id)===String(v)`。`saveLocal()`はS全体を直列化(**新フィールドは自動で永続化・同期・エクスポート・スナップショットされる**)。セッション/麻雀の保存は`Object.assign({},draft)`のスプレッド方式なのでドラフトに足せば保存も自動で通る。`save()`=saveLocal+toast+同期スケジュール+updatedAt更新。`num()`=parseInt系、`fnum()`=parseFloat系。

## 3. カラーパレット & デザイン体系(確立済み・変更しない)
```
--bg:#0B1F1E --surface:#12302E --surface2:#1A3E3B --line:#24504C
--amber:#F2B33D --teal:#2DD4BF --red:#E57373 --text:#E8F0EF --muted:#8FAAA7 --blue:#5FA8E8
```
- **色の意味体系**: 緑=プラス/推奨、赤=避けるべき/消費、オレンジ=中間。家計簿カテゴリ(消費=赤枠/娯楽=橙枠/収入=緑枠、枠線のみ)、スロットモード(乗打=橙/代打=緑/単独=赤)、麻雀(セット=橙/フリー=赤)、平均着順の色は基準線(rc+1)/2比較。
- **予算バーの文法(v3.88-3.92で確定・蒸し返さない)**: 左=ラベル(muted)+「使った / 上限」(10px muted) / 右=**語(残り・あと・超過/muted)+ 金額(17px太字・バーと同色)** + 「›」 / 下=細いバー(8px)。**金額の色はバーと連動**——変動消費=赤/内枠=薄い赤/娯楽=琥珀/貯金=teal。超過時も分類色を維持し、区別は語とバーが満杯になる形が担う。**カード内に白の金額を置かない**(このアプリは金に色を付ける体系なので無彩色は体系外で浮く)。カードは「予算(使う)」と「先取り(移す)」の2枚に分離。
- タグ=アウトラインピル統一(10px、副次タグ9px)。カード表示は3画面統一(日付ヘッダー+同日1カード+border-top区切り、一次11px muted/二次11px op.7/金額14px)。人物ピル=personTag()。
- **アプリアイコン=画線法(tally mark)確定済み**: 縦4本白+斜め1本琥珀(右肩上がり)、ダークティールグラデ。apple-touch-icon.png?v=4。遊技タブアイコン=**フラット正面5の目サイコロ**(線画1.7px、他タブと様式統一。旧アイソメ立体は「ピザに見える」問題で廃止)。

## 4. レイアウト構造(重要・壊さないこと)
- タブ: 家計簿(ledger・初期タブ)/遊技(games)/資産(assets)/ツール(tool)。
- **flexbox骨格**: `body{height:100dvh;display:flex;flex-direction:column;overflow:hidden}`、`#app{flex:1;overflow-y:auto}`がスクロール領域、`#navbar`は#appの兄弟でフロー最下段。position:fixedナビは廃止済み。
- **render()は_renderBody()のラッパー**: UI.tab+"/"+UI.screen+"/"+UI.gameSubが前回描画から変わった時だけ#app.scrollTop=0(タブ/画面遷移でスクロール深度が残るiOSバグ対策・v2.44)。同一画面の再描画は位置保持。renderKeepScroll()は明示的に保持したい時用。
- **ナビ下余白は適応式(v2.46)**: `:root`の`--vgap`(JSのupdVgap()が起動/resize時に screen.height−innerHeight を実測代入)と`--navpad: max(2px, calc(env(safe-area-inset-bottom,0px) - var(--vgap) - 12px))`。navbar/FAB/toastが--navpad参照。**-12pxが位置調整の唯一のノブ**。
- **保存インジケーターはnavbar内・タブ直下**(v3.27、`tabBar()+行`の連結順)。**navbar上端にフェード帯**(`#navbar::before`、上へ24px張り出す透明→bgグラデ・pointer-events:none・v3.28)+padding-top6px——コンテンツはタブピルに激突せず溶けて消える。透明側はbg同色のalpha0(Safariのtransparent=黒混ざり回避)。navbar実高は毎render`--navh`へ実測注入(v3.03)なので構成変更しても#app下pad/トーストは自動追随。ノブ=フェード高(top:-24pxとheight:24pxの2箇所連動)。※iOSが62pt問題を直すとインジケーターがホームバーへ接近する——窮屈に見えたら戻すか消すかその時判断。
- **★iOS 26の62pt問題(結論済み・再調査不要)**: スタンドアロンPWAのレイアウトビューポートが物理画面より約62pt短い(実測 inner=812/scr=874/sab=34)。fixed/フローとも62pt持ち上がる。manifest display:fullscreen実験→不発。**OS予約領域でWeb側からは越えられない=iOS修正待ち**。iOSが直れば--navpad機構が自動で適正位置(下端22pt)に降ろす。この件で追加のCSS実験をしないこと。

## 5. データモデル(S, localStorage KEY="kakeibo:v1", Supabase同期)
- `S.entries`: 家計簿 `{id,date,type,category,amount,memo,src,fixedId,store,item}`。src="slot"/"mahjong"=遊技連携(linkId="mj-"+id等), "daida"=代打ち, "fixed"=固定費。
- `S.sessions`: パチスロ `{id,date,store,mode[nori/dai/solo],partnerName,fee,exchange,pExchange,pushPayout,pushDraw,machines[],expenses[],memo,`**`startAt,endAt`**`}`。**startAt/endAt(HH:MM文字列・任意)はv2.48で追加。時給分析用。カード等には表示しない(記録のみ)**。
- `S.mjSessions`: 麻雀 `{id,date,store(ルール名),category[free/set],rule[3/4],score,chips,scoreRate,chipRate,ranks[4],players[](同卓者最大5),baseFee,topBonus,drinkFee,setFee,memo,fromTable,`**`startAt,endAt,expenses[]`**`}`。**expenses[](v2.49)=セット飲食等の私的経費 {id,label,amount}、スロットと同型UI。mjFinal = mjRaw − mjGameFee − mjExpTotal**。
- **共有/私的の境界(v2.49-2.51・壊さないこと)**: スコア表(mjTables)は同卓者に見せる共有ドキュメントなので**金(経費)のUIは置かない**。収支反映は既存生成セッションを削除→再生成する方式だが、**私的フィールド(expenses/memo)はprev[0]から引き継いで生き残らせる**。卓由来の数字(スコア/チップ/ゲーム代/着順)は再生成対象。
- **時刻は表が所有(v2.50-2.51)**: 時刻は同卓者全員が知る共有情報なのでスコア表に**「プレイ時間」欄**(日付行の直下、9pxラベル上置きの様式)を表示・編集可。**共有画面では「実働」等の稼働管理語彙を使わない**(セッション側の私的フォームは「実働」のまま)。`t.startAt`=全席の名前が既定文字(A/B/C/D)以外で埋まった瞬間に自動記録(1回のみ)、`t.endAt`=初回の収支反映時に自動記録。**反映のたび sess.startAt/endAt = 表の値をコピー**(表で直せば再反映で記録に伝播)。再反映で終了時刻が現在時刻に更新されることはない(表の既存値を尊重)。深夜跨ぎは end<start なら+24h解釈。
- 計算: machSabai=end−start+hold、machYen=round(差枚/ex*1000)−現金、finalOwn=grossOwn−経費、mjFinal=mjRaw−mjGameFee、mjAvgRank。換金率は**全て1000円基準**(schemaV=3)。
- `S.mdb`(機種), `S.stores`(店舗プリセット), `S.fixed`(固定費), `S.wishlist`, `S.bank`, `S.savings`, `S.budgets`, `S.mjTables`(スコア表), `S.mjPresets`, `S.borderTool`, `S.schemaV:3`, `S.updatedAt`(LWW用・save()でDate.now())。
- **applyDataがスキーマ移行含む唯一の正規復元経路**。インポート(bimport)もスナップ復元もクラウド採用も全てapplyData経由(v2.40でbimportの手動代入バグを修正済み——二度と手動代入に戻さない)。

## 6. バックアップ体制(v2.40-2.47で構築・検証済み)
**三層防御。全て実弾検証済み(訓練復元+実際の端末初期化からのクラウド復旧に成功)。**
1. **ローカル自動スナップショット3世代**: SNAP_KEY=KEY+":snaps"。起動時に日付が変わっていれば「日次」、インポート/スナップ復元前に「復元前」、クラウド採用前に「同期前」。**空データ(entries/sessions/mjSessions/mjTables全て0)は退避しない**(v2.47)。takeSnap()は容量不足時に最新1世代へ縮退。UIは折りたたみ式(UI.snapOpen、既定閉・1行サマリ)。復元は確認ダイアログ+復元前自動退避付き=誤爆しても不可逆損失なし。**アイコン削除=localStorage全消去でスナップも消える(仕様)→クラウドが救う**。
2. **クラウド**: Supabase app_data(1行/ユーザー、LWW、S.updatedAt比較)+**サーバ側履歴 app_data_history(1時間1世代・最新20世代、security definerトリガーtrg_tally_snapshot)**。**RLS有効・全ポリシー auth.uid()=user_id を2026-07-12に実機検証済み**(own_*とtally_own_*が重複しているが無害)。復元手順は RESTORE_RUNBOOK.md(§3のSQLでjsonb_setによりupdatedAtを現在化してLWWで必ず勝たせる)。
3. **手動エクスポートJSON**: 機種変更・iOS大型更新の前のみ。保存先はiCloud Drive「Tally」フォルダ固定を推奨。

- バックアップ画面の並び(使用頻度順・v2.41): クラウド同期→スナップショット→自己診断→エクスポート→インポート→バージョン表記。「アプリの更新」セクションは削除済み(自動チェック: 起動1.5秒後+前面復帰時60秒スロットル、手動: ヘッダー同期ボタン)。
- **自己診断(btest)**: 保存領域R/W、本体件数、計算検証(差枚500/スロ収支-10000/セッション-11500/麻雀2400/**麻雀経費込み2100**/平均着順1.75/スキーマ3——期待値は実関数で検証済みのフィクスチャ)、書出→再取込の件数・金額合計一致、画面計測(inner/client/vv/scr/sab/fixBtm/bodyBtm/dpr)、スナップ世代数。結果は×で閉じられる(msgclose)。診断・スナップ復元はrenderKeepScroll。

## 7. 直近チャットの作業ログ(v2.37→v3.87)
- v2.38 アプリアイコン画線法化(?v=4) / v2.39 遊技タブをフラット5の目に
- v2.40 **復元バグ修正(bimport→applyData化)**、自動スナップショット3世代、自己診断拡張
- v2.41 バックアップ画面を頻度順に再構成、更新セクション削除 / v2.42 診断結果に閉じる×+スクロール保持 / v2.43 スナップ一覧折りたたみ
- v2.44 **タブ/画面遷移のスクロール残留バグ根治**(render()のloc判定)
- v2.45 画面計測を診断に追加 / v2.46 **適応型ナビ余白(--vgap/--navpad)**+manifest fullscreen実験 / v2.47 実験不発確定→manifest standaloneへ復帰+?v=2キャッシュバスト+空スナップ抑止
- v2.48 **セッション実働時刻(startAt/endAt)を両フォームに追加**(時給分析の前提データ収集開始)
- v2.49 **麻雀セッションに私的経費(expenses[])**: スロット同型UI、mjFinalに反映、再反映での私的フィールド消失バグを同時修正
- v2.50 **スコア表の時刻自動記録**: 名前入力完了→startAt、初回反映→endAt(再反映では不変)。nowHM()ヘルパー新設(※todayStrは複数行関数——直後への挿入時はスコープ事故に注意、実際に一度やらかして修正した)
- v2.51 **スコア表に時刻欄を表示・編集可に**: 時刻の所有権を表へ移管(反映は常に表の値をコピー)。t.endAtフィールド新設
- v2.52 表の時刻欄を「プレイ時間」に改称しレイアウトを日付行の様式(ラベル上置き・高さ44px)に統一
- v2.53 **iOSがtype=timeへ大型ネイティブ描画を当てheightを無視する問題**を `input[type="time"]{appearance:none}` で抑止(dateは従うのにtimeは従わない非対称。今後time入力を足す時もこのCSSが効く前提でOK)
- v2.54 **家計簿→セッション経費の振替**(先に家計簿へ記録した移動費等を、後からセッション経費へ移して元記録を保存時削除)
- v2.55 経費行を家計簿と同じ**店名/品目**の2分割に(旧labelは品目へフォールバック表示・編集で自然移行。振替は店名/品目を無劣化で運搬。履歴補完チップは経費行では意図的に不採用——振替が補完役のため)
- v2.56 振替を含む保存時にトーストで件数を明示。**「振替元をグレーアウトで残す」案はユーザーと議論の上で却下済み**(集計除外の分岐が全箇所に必要になり二重計上リスク、墓標は財務事実でなく編集メタデータ、追跡はfromEntry+移動先の店名/品目で担保——蒸し返さない)
- v2.58 スコア表画面を白基調に(実戦フィードバック) → v2.60でスコープ変更
- v2.59 支払行のバグ修正: ゲーム代0の人も「誰かにゲーム代があれば」支払表示(anyFee方式・印刷版と統一)。ライブ更新側に残っていた旧様式(「受取」表記・琥珀色)の複製も描画版と同一ロジックに統一
- v2.60 **対局モード(mjplay)新設**: 実戦で「入力はスコアだけ・経過が見たい・字が小さい」との同卓者評を受け、白基調・大文字のスコア入力専用画面を追加。viewMjPlay+既存bindMjTable/キーパッド(mjtKeypadHtmlとして抽出)を完全共用(padInputが全キーrenderKeepScroll完結なので結合なし)。清算画面(mjtable)はダークに復帰、sheet-lightはmjplayのみ。導線: 清算画面の「▶対局モード」⇄「‹設定・精算」。清算の共有は既存の「PDFで共有/印刷」ボタン(mjtPrintHtml+window.print)
- v2.61 v2.60のバインド漏れ修正(スコア表画面のイベントは**data-mt属性**で束ねる。data-aは別画面用——新要素追加時は要注意)
- v2.62 対局モード: プラスとオートフィルを黒(紙のスコア表の慣習・青は使わない)、ヘッダーをsticky化し合計+着順内訳(1着·2着·3着の回数)を常時表示。PDF精算票に**スコア推移の累積折れ線(SVG自前実装・ライブラリ不使用)**を追加。**チップはグラフに混ぜない方針で確定**(半荘ごとのチップ推移データが存在せず、平均で均すと捏造になる——ユーザー了承済み、蒸し返さない)
- v2.63 対局モード実戦2回目の改善: ①stickyは親のpadding領域へ入れない仕様のため行がヘッダー上へ透けていた→body.sheet-light #appのpadding-top:0+thead先頭にsafe-area高の不透明キャップ行+戻る「‹」をヘッダー左端に内蔵(常時可視化) ②キーパッドを選択中半荘の直下のtr(colspan)に展開(最下部固定を廃止) ③PDF: @page margin:0でSafariが余白に刷るURL/日付を抑止(.pr-wrapがpadding:10mmで内側余白を担保)+.pr-breakで「半荘表」/「グラフ+集計」の2ページ構成
- v2.64 清算画面のスコア表グリッドを撤去(対局モードに一本化)。名前入力はband("スコア")のグリッドへ移設(**pname=開始時刻トリガーなので消さないこと**)、プレイヤー追加/列削除も同ブロック、行削除はキーパッド内「この半荘(行)を削除」(rowdelcur)へ。PDF集計表(pr-sum)に名前ヘッダー行を追加(改ページで半荘表と分離され名前が不明になったため)。- v2.65 清算画面の最終形: 成績表の上部固定を解除(旧スコア表時代のmjt-stickyクラス除去)、名前入力をチップ表ヘッダーへ統合(band「プレイヤー・チップ」、×削除/＋列追加も同居)、対局モードボタンはその直下。清算画面の構成: 日付・ルール→成績→プレイヤー・チップ→対局モードボタン→ゲーム代→家計簿・共有
- v2.66 ①キーパッド再設計: スコア時のミニプレイヤー行と「入力中の行」ラベルを撤去(直上の実テーブル行と重複)、[数字4段(20px)]→[抜番/飛び(細)]→[◀ 完了 ▶]→[行削除(細)]の順に再配置 ②**精算シート画面(mjsheet)新設**: 印刷用HTML(mjtPrintHtml)をそのまま画面表示する白い紙面。導線=共有バンド「精算シートを表示」⇄戻る、画面内にPDF保存ボタン。**スクショ共有=URLの写らない画像共有の実用解**(pr-*の見た目CSSは@media print外へ移設して画面共用、改ページ系のみprint内に残置)
- v2.67 ①mjtPrintHtml(t,mode)化: **print=過程→グラフ→結果 / screen(精算シート)=結果→直近半荘(pr-latestで強調)→グラフ→全過程**の章順分岐 ②グラフ刷新: 中間点廃止・線2.5px・切りの良い目盛り・**凡例廃止し線右端に名前直付け(縦11px間隔で衝突回避)** ③セル選択時にキーパッド全体へ自動スクロール(scrollIntoView nearest) ④キーパッド内に精算シートボタン、シートの戻り先はUI.sheetFromで記憶(対局→シート→対局)
- v2.68 精算シートの視認性修正: sheet-lightをmjsheetにも適用(座標系を対局モードと同一世界に統一)、ヘッダー上余白にmax(env,54px)の床(時計との衝突防止)、pr-no/pr-lblのnowrap、**#appスコープのCSSで画面表示のみ文字拡大**(12.5px/支払14px/タイトル20px——#print-area経由の印刷は不変)、グラフx目盛りの末尾重なり間引き
- v2.69 シート最終調整: **pr-tblをtable-layout:fixed+ラベル列56px固定**(列幅が内容で変わる問題と右はみ出しを同時解決)、画面表示のwrap余白を14px/8pxへ圧縮、セル12px・overflow:hidden、グラフ減音(線2px・罫線#ececec・終点2.5px)。**スコア表の「半荘」表記を「◯回戦」へ一般化**(東風戦等の非半荘ルール対応。セッション記録側のhanchan/半荘数の語は対象外で維持)。章順(結果→直近→グラフ→全過程)はユーザー確定
- v2.70 **シート三層化の完成形**: 途中経過シート(mode="mid"・キーパッドの「途中経過を表示」から・平均着順/着順内訳/合計点のみで**金情報とPDFボタンなし**) / 清算シート(mode="screen"・清算画面の共有バンドから・全情報) / 清算画面は成績表を撤去し操作盤に純化(ライブ更新のmjt-sc等はifガード済みで無害)。履歴表はNo→「◯回戦」表記+pr-histクラスで縞模様(偶数行#F2F6F5、直近行は除外)。**対局中に金額を見せない境界**はユーザー設計思想——尊重すること
- v2.71 対局モード末尾(回戦追加の下)に途中経過への直接ボタン(キーパッド経由不要に)。**紙面の抜け番表記を「–」(グレー)に統一**——アプリ既存の欠損表記と同語彙。対局モードの「抜」は抜番キーの入力エコーとして意図的に維持。グラフと履歴表の間に16px、シート上部のスクショ説明文は撤去
- v2.73-75 **端数処理(mjtSettle・唯一の実装点)**: v2.75で**場代切捨は廃止**(ユーザー訂正——切捨対象は最終支払額のみ)。切捨=なし/100/500/1000円(t.roundPay、旧true=100互換)。**v2.78確定ルール: トップ(最大受取)以外は全員——負けも2位以下の浮きも——単位切捨、端数の帳尻は全てトップが引受**(卓上の小銭をトップに集約する設計・ユーザーの家ルール)。受取側ゼロなら適用なし。シートには「調整」行で各自の増減をセル表示、注記はルール文のみ。t.roundFee/feeBearersは死にフィールド(無害・触らない)。**帳尻(Σ支払=−場代合計)は常に一致**、シート注記に負担者/調整を明記。UIはゲーム代バンドのトグル+負担者チップ。**収支反映はsetFee=収支−精算後支払の逆算**(端数OFF時は生場代に一致するため分岐なし・家計簿は常に実額)。清算シートから直近テーブル撤去(途中経過専用に)
- v2.75 **清算画面の縦積み転置**: プレイヤー・チップ表=1人1行[名前入力(+×削除)/開始/終了/増減](プレイヤー増は縦に伸びる・横スクロール撤廃)、＋追加は表下のボタンへ。場代入力も1人1行のカード。**可変軸(人数)は縦に流すのがこの画面の設計原則**
- v4.43 **保存ボタンを全画面で下バーの「戻る」の右へ統一(ユーザー指摘)**。対象は`稼働を編集`(ssave)/`麻雀を編集`(mjsave)/`固定費`(fsave)/`店`(stsave)/`機種DB`(fsave)。**フォームが開いている画面(固定費・店・機種DB)は、開いている時だけ出す**(一覧表示中は`戻る`だけ)。ラベルは状況に応じて`保存する / 登録する / 更新する / 追加する`。実装は`subnavExtra(sc)`が本文の隠しボタンをclick()するだけで処理は二重にならない。
  あわせて**フォームを閉じる操作の名前と見た目を統一**——同じ操作に`やめる / キャンセル / 戻る`と**3つの名前**が付いていたので**「やめる」**に揃え、全幅・mutedのボタンにした。保存が下バーへ出た結果、本文に残るのはこの1つだけになる。
  **【画面の骨格が確定】** `ヘッダ(‹戻る / タイトル / 削除)` → `本文(入力とチップ、末尾に稀な操作のテキストリンクと「やめる」)` → `下バー(‹戻る / 画面固有 / 保存)`。新しい画面もこの型に従う。
- v4.42 **品目行の金額欄の幅が1件目と追加行で違っていた**(ユーザー指摘)。1件目は`width:28px`のスペーサー、追加行は`.xbtn`(幅指定なし=内容+paddingで約23px)で、**削除枠の実幅が一致していなかった**。両方を`width:24px;flex:0 0 24px`に固定し、入力欄には`min-width:0`を付けて縮まないようにした。**行が増えても金額欄の位置が動かない**。
- v4.41 **【長年の余白バグ】`#app`の下余白が navbar 1個分ぶん二重取りされていた**(ユーザー指摘: 無理に下へスクロールできて無駄な空白が出る)。`padding-bottom: calc(max(var(--navh),60px) + 20px)` = 84〜110px を持っていたが、**`#navbar`は`position:fixed`ではなく`flex:0 0 auto`の兄弟**で、そもそもスクロール領域の外にある。必要なのは`#navbar::before`のフェード帯(上へ24px張り出す)にコンテンツが潜らないぶんだけなので**28pxへ**。
  **全画面で表示できる高さが80px前後増える**。navbarが`display:none`の画面(mjsheet等)でも28pxで足りる。`--navh`自体は`.toast`の位置決めに使うので残す。
  **教訓: 「余白が余る」は要素の配置方式を疑う**。fixedを前提にした余白計算が、in-flowに変わった後も残っていた典型。
- v4.40 **固定費への登録を下バーから降ろす**(ユーザー指摘: 削除より使用頻度が低い)。v4.39で下バーに入れたが、**親指の一等地を占めた上に「戻る」と「更新する」の間に挟まって誤タップを招く**位置だった。ボタンの箱を持たない**テキストリンク**にして本文の末尾へ。
  **【置き場所の規則が確定】** ①**毎回使う**=下バー(記録する/更新する/振替) ②**複数画面に共通し位置を覚えたい**=ヘッダ右端(削除) ③**この画面固有で稀**=本文末尾のテキストリンク(固定費へ)。**削除が稀なのにヘッダにあるのは、複数画面で位置を揃えたいから**であって頻度ではない——この2軸を混同しないこと。
- v4.39 **削除ボタンを各画面のヘッダ右端へ統一(ユーザー設計)**。**滅多に触らない操作のために最下部までスクロールさせるのは無駄**で、位置が画面ごとに違うのも覚える対象を増やす。対象は`記録を編集`(edel)・`稼働を編集`(sdel)・`麻雀を編集`(mjdel)の3画面。`.navbtn`と同じ見た目で色だけ赤。新規作成時は従来通り空のスペーサ。
  あわせて**「この内容で固定費に登録」を下バーへ**(編集中のみ)。記帳画面の下バーは`戻る | 固定費へ | 更新する`の3分割。**本文の末尾に並んでいた縦積みのボタン群が消えたので、削除の下にできていた無駄な余白も解消**。
- v4.38 記帳画面のレイアウト(ユーザー指摘)。①**日付を最上段へ**——いつの記録かが先に決まる方が読み順として自然。金額は品目行にあるので、上段に残すのは品目行を使わない振替/交通コンポーザのときだけ。並びは`日付→タイプ→分類→財布→店名→品目`。②**財布のボタンを等幅グリッドに**(`repeat(auto-fit,minmax(72px,1fr))`)——**名前の長さでボタンの大きさが変わると押す位置が毎回ずれる**。財布が増減しても等幅のまま折り返す。
- v4.37 **保存ボタンを下バーへ移し「追加する」→「記録する」に改名**(ユーザー指摘: 「品目を追加」と意味が被る)。`SUBNAV_EXTRA`を`subnavExtra(sc)`の関数に変え、**ラベルを状況で出し分ける**(新規=記録する / 編集=更新する / 振替=振り替える)。本文にはdisplay:noneで置いてハンドラの担い手だけ残す(v4.27と同じ手法)。削除ボタンは本文に全幅で残置。
  **品目の粒度についての結論(ユーザー質問「Yシャツ99円×3にすべきか」)**: **分けない。1レシート行=1エントリ**。理由は単価の情報量ではなく**チップの再利用**——品目名に数量を入れると「Yシャツ 3点」「Yシャツ 2点」が別チップになって候補が断片化し、摩擦を減らすという設計目的に反する。数量は品目名に入れず、必要なら金額と回数から推定する。**エントリを分ける基準は「別々に分析したいか」**で、同一商品の複数点は分析上1つ。
- v4.36 ①**品目と金額を同じ行に**(ユーザー指摘)。v4.35は1件目の金額だけが画面上端(日付の隣)にあり、**追加行を埋めた後に上へ戻る必要があった**。1件目も追加行とまったく同じ形にして上から下へ一直線に埋められるようにした。上段は日付だけ(振替と交通コンポーザは品目行を使わないので金額を残す)。
  ②**レシートの貼り付け取り込み(ユーザー提案)**。レシートを撮ってClaudeに読ませ、返ってきた文字列を貼るだけで行が埋まる。**アプリ内にOCRもパーサの知識も持たない**のが要点——店名の表記ゆれ、分類の判断、金額の拾い方はモデル側が引き受けるので、**アプリ側は決まった書式を受け取るだけで保守が要らない**。
  **書式**: `#で始まる行`=ヘッダ(date/store/wallet/category/type)、それ以外は`品目 | 金額`。**区切りが無ければ行末の数字を金額**とみなす。全角パイプ・カンマ区切り・¥記号・全角空白に対応。金額0の行(「合計」など)は捨てる。
  **取り込みはフォームを埋めるだけで保存しない**——OCRの精度は完璧にならないので必ず目視で直せるようにする。財布名は`walletPots()`から引いてidに解決する。
  不変量テストに【11】(ヘッダ解析/区切りの各種/フォーム反映/保存しないこと)を11項目追加。invariants は61→72項目。
- v4.35 **同じ店で複数の品目をまとめて入力(ユーザー設計)**。レシート1枚=1回の入力にする。`f.extra=[{item,amount}]`の行を「＋品目を追加(同じ店)」で増やし、2件以上で合計を表示。
  **保存は行ごとに別のエントリを作る**(日付/分類/店/財布は共有)——1件にまとめないのは、**品目ごとの分析(ジャンル判定・内訳ドリル・店舗別)を壊さないため**。金額が空の行は無視する。出すのは**新規の支出・収入のみ**(編集中は既存1件を分割できない/交通コンポーザ中/振替中は出さない)。
  **【ミス】`extraHtml`を`const isTr`より前に置いてTDZエラー**——記帳画面が丸ごと落ちる状態だった。位置を移して解消。**描画スイープのentryフィクスチャに`extra`を追加**して再発を検出できるようにした(それまでは`extra`が無かったので通過していた)。
  **レシートOCRについて**: iOSショートカットの`画像からテキストを抽出`が**完全オフラインで動き日本語精度も実用域**なので実現可能。Tesseract.js等をアプリに載せる案は10MB超のWASMが要りオフライン単一ファイルの前提を壊すので却下。ただし**OCRは「複数品目フォームを事前に埋める」機能**なので、フォーム(本項)が先。精度は完璧にならないため手直しできるフォームが必須。実装するなら「クリップボードから貼り付け→行に展開」の形。
- v4.34 **パチスロと麻雀のセッションを1つの一覧に統合(ユーザー設計)**。v4.32で麻雀の全期間集計をツールへ移した結果、**両サブタブに残ったのは構造が完全に同型のセッション一覧だけ**になり、分ける理由が消えた。時系列で混ぜて並べる方が「その月に何をしたか」を素直に読める(ヘッダーは元々パチスロ/麻雀の両方を出していた)。**新規作成の種別はボタンを2つ並べて解決**——`[＋ パチスロ][＋ 麻雀]`。ダイアログを挟むより速く説明も要らない。
  `viewSlot`/`viewMahjong`を廃し、行の描画だけ`gameRowSlot`/`gameRowMj`に残して`viewGameSessions()`が日付でまとめる。サブタブ(`UI.gameSub`)・`gamesub`ハンドラ・スクロール記憶キーの`gameSub`部分も全て撤去。
  **【重大なミス】範囲削除で`personTag()`を巻き込んで消していた**——`viewSlot`〜`viewMahjong`をまとめて置換したが、**その2関数の間に`personTag`が挟まっていた**。復元済み。**教訓: 関数を範囲でまとめて置換する前に、その範囲に他の定義が入っていないか必ず確認する**(元ファイルとの関数名の差分を取れば一発で分かる)。描画スイープの`sessions`に`partnerName`付きを足して再発を検出できるようにした。
- v4.33 **ルール名別 成績を2階層へ**(ユーザー設計)。①説明文を撤去。②**ルールが増えると1画面に全部の詳細を積むのは無理**なので、**一覧(ルールを選ぶ)→詳細**に分けた。一覧は要約のみ(半荘数/日数/平均着順/平均収支)。③詳細で**累計と月別の両方**を同じ様式で見せ、月は‹›で切り替えられる。**記録のある月だけを行き来する**(空の月を延々めくらせない)。
  **集計本体を`mjAgg(list, mk)`に切り出した**——期間を渡すだけで全期間にも特定月にも使える。描画は`mjAggHtml(A)`で共通化。累計と月別で表示が食い違う余地が構造的に無い。描画スイープに`mjstatd`を追加(120→125)。
  ※4麻の2着の色にあった`var(--blue)`は`var(--amber)`へ、3着は`#B39DDB`に変更(v4.31の青撤去の続き)。
- v4.32 **「ルール名別 成績(全期間)」を遊技タブからツールタブへ移設**(ユーザー指摘)。**遊技タブは月セレクタの下に月の記録を並べる画面なのに、全期間の集計が同居していた**のが構造の矛盾。しかもパチスロ側には無く左右が非対称だった。**遊技タブ=記録 / ツールタブ=分析**で役割を分ける。ツールの麻雀バンドに導線を置き、専用画面`mjstats`を新設(集計本体`viewMjStats()`はそのまま再利用)。描画スイープの対象画面にも追加。
- v4.31 麻雀の平均スコア/平均チップの色を**青→teal**へ(ユーザー指摘: 青は使っていない)。`mjStatRow`のプラス側が青だった。
  **【作業中のミス】未使用と判断して`--blue`変数ごと削除しかけた**——`.blue`クラスと`.mjt-cell.blue`は確かに死んでいたが、**`var(--blue)`は10箇所で直接参照されていた**(承認カテゴリの色、予定タグ、4麻の2着、スコア表のプラス、wishlistの状態色など)。即座に復旧。**教訓: CSS変数を消す前は`var(--名前)`での直接参照も必ず数える**——クラス定義の使用箇所だけ見ると死んで見える。
  **青の現状**: 実際に画面へ出るのはwishlistの状態色とURLリンクのみ(承認カテゴリと予定タグは該当データが無いため未出現)。**色の体系はteal/amber/redの3色**という原則からすると青は例外なので、いずれ整理する余地がある(今回は指摘のあった箇所だけ変更)。
- v4.30 資産タブの並びを **所持金→貯金→軍資金→投資** へ(ユーザー設計)。**情報量の多い順であり、使う頻度の順でもある**——軍資金は構造上どうしても薄く、投資は2027年開始まで使わない。上から順に見て、下ほど「今は見なくていい」になる並び。
  **並び替え機能は保留**(§8の却下ではなく保留)。カード4枚では効果に対して実装が重く、しかも**いま最適な並びが決まっているなら並び替えは要らない**。投資を開始して4枚とも常用するようになり、かつその時点で最適順が人によって変わるなら再検討する。
- v4.29 資産タブの所持金の内訳に**構成比バーと色見本**を追加(ユーザー指摘: しょぼい)。v4.28は名前と金額を並べるだけで、**貯金は目標バーを持っているのに所持金は何も持っていなかった**のが薄さの正体。**構成比という情報を既に持っているのだから使う**——貯金の目標バーと同じ位置に構成比の帯を置き、色見本を凡例と対応させた。**所持金の財布は固定メンバーなので多色**(v4.26の判断と一貫)。
- v4.28 ①「遊技の勝ち負け 累計」→**「遊技収支 累計」**(ユーザー指摘: 勝ち負けという表現が違和感)。月別内訳の「遊技」も「遊技収支」へ。②**資産タブの所持金カードに財布の内訳を追加**——貯金は箱の内訳を出しているのに所持金は出していなかった。**どちらも複数の入れ物を束ねたカード**なので様式を揃える(財布が2つ以上のときだけ表示)。
  **【実データ監査】ユーザーから「ソニー銀行の残高がズレた、バグはないか」と報告あり。8項目を監査して整合性に問題なし**(財布別合計=総額 / 存在しない財布を指すエントリ0 / 財布未設定0 / ID重複0 / 保存復元で総資産不変 / 未来日222件が現在残高に混入していない)。**ズレの原因はカード払いの決済タイミングと、財布を作る前の記録が全て先頭の財布に寄っていること**——どちらも仕様の範囲で、調整で合わせる運用が正しい。所持金は「帳簿上の残高」であって実口座残高ではない(v4.22で説明文は撤去したが挙動は不変)。
- v4.27 軍資金の整理(ユーザー指摘)。①**上限のバーを撤去**——残高が上限のどこにあるかを見ても行動は変わらない(**40万勝てば上限という概念自体が意味を失う**)。②**「残りわずか」の警告を撤去**——「少ないから何?沢山補充するの?」という指摘の通り、**何をすべきかを示さない警告**で、しかも促す先が漏れそのもの。③数値は**「振替目安」**として残した——**超過分を貯金へ回す仕組み(+EVの回収)は設計の中核**なので落とせない。「上限/天井/上限超え」の語は全て「振替目安」へ。資産タブの軍資金カードも同様(バー撤去、目安超えの時だけ一行出す)。
  ④**編集/調整を下バーへ移動**。`SUBNAV_EXTRA`で画面ごとに追加ボタンを定義できるようにした。実装は**本文にdisplay:noneで置いたボタンをclick()するだけ**で、処理を二重に書かない(v3.96の戻るボタンと同じ手法)。軍資金の下バーは`戻る|編集|調整|振替`の4分割。**戻るが細るので最大4つまで**に留めること。
- v4.26 貯金画面の整理(ユーザー指摘)。①説明文を撤去。②**構成比グラフの多色をtealの濃淡へ統一**——**赤は超過・危険に予約された色**なので単なる識別に使うのは体系違反(v3.92の色の体系)。加えて**箱は増減するので個別の色を覚えても意味がない**。**所持金の財布は固定メンバーなので多色のまま**にした(あちらは色で見分ける価値がある)——同じ構成比グラフでも、要素が固定か可変かで最適な配色が違う。③**各箱の金額を白から色付きへ**(v3.92の「金額の色はバー連動」に反していた): 達成=teal / 未達=amber / 目標未設定=muted。箱名はmutedへ落として白は使わない。目標未設定の箱には「目標額 未設定」を明示。
- v4.25 ①**振替累計の「出/入」内訳を撤去**(ユーザー指摘)。**振替は金の移動なので差分以外に価値がない**——内訳が分かっても行動は変わらない(収支とは違う)。`bankTransfer()`は内部で出入りを持ったまま、表示はnetのみ。②**軍資金の残高カードを中央揃えへ**——貯金・所持金は中央揃えなのに軍資金だけ左右分割で、残高が右端に寄って間延びし、操作ボタンだけが右に浮いていた。3画面で残高カードの様式が揃った。
- v4.24 軍資金の集計カードを作り直し(ユーザー設計)。①**開始時の残高を撤去**(所持金の起点と同じ理屈——一度決めたら調整で上書きされるので残す意味がない)。②**「貯金に回した累計」を「振替 累計」へ**——旧表示は`sweep`だけを見ており、**いくら回収したかは分かってもいくら注ぎ込んだかが見えなかった**。`bankTransfer()`で 入(topup) と 出(sweep+return) の双方を測り、**net = 出 − 入 の差分で語る**。プラスなら持ち出しを回収できている、マイナスなら食われている、が一目で分かる。③「今月の補充」→**「今月の振替(入)」**。
  **語彙の統一を完了**: 画面上の「補充」を全て「振替」へ(振替枠 / 今月の振替(入) / 月別内訳の「振替入 / 貯金へ / 所持金へ」)。月別内訳は**向きと行き先が読める**書き分けにした。未使用になった`bankSaved()`呼び出しは撤去(v3.55)。漏れ係数画面の「外部から補充」も「軍資金へ振替」へ。
  不変量テストに`bankTransfer`の2項目(net=出−入 / 補正は出入りに数えない)を追加。
- v4.23 所持金の**月末見込みの常設表示を撤去**(ユーザー指摘)。**振替をすれば動く数字なので、常に見せても判断が変わらない**。見るべきなのは「このままだと払えない」という一点だけで、それは`walletForecast().short>0`の警告(v3.81)が既に担っている。警告の文言も「貯金から引き出すか」→「**貯金から振り替えるか**」へ語彙統一。`walletForecast()`自体は警告のために残す。
- v4.22 所持金画面の表示整理(ユーザー指摘)。①冒頭の説明文3行を撤去——**毎日見る画面に毎回同じ説明は要らない**。②「いまの所持金」→**「残高」**。③**各財布の「起点 ¥X」の常設表示を撤去**——起点は一度決めたら以後は調整で上書きされるので、残り続ける意味がない(値は編集から確認・変更できる)。
- v4.18〜v4.21 **遊技タブのアイコン(サイコロ)の作り直し**。ユーザーの指摘が3段階で入った: (a)3の目を右斜め下→左斜め下・2も同じ向きへ・気持ち拡大 → (b)**「凝視すると立方体でなく平面の六角形に見えてくる」** → (c)**「目の間隔が広すぎる」**。
  **(b)の原因は正六角形であること**。正六角形は6回対称が強く、内部のY字(3回対称)と競合してネッカーキューブ的な反転が起きる。実物の写真がそうならないのは正対していないから。**手打ちの座標をやめ、ヨー/ピッチを与えた実際の3D投影から輪郭・稜線・目を生成**するようにして解決(左右の面幅が非対称になり六角形として読めなくなる)。**角を半径1.3で丸める**のも「図形でなく物体」に寄せる効果あり。
  **(c)は実物を実測して比率を合わせた**。実物は中心間≒直径×1.26、旧実装は1.59。**間隔を詰めるだけでなく目を大きくして**1.33へ。22px表示では隙間1.15単位(≒1.05px)が下限で、それ以下だと2点が繋がって見えるため実物の1.26には到達できない。ヨーは58°→53°——58°では右面が潰れて2と3の目の大きさが47%食い違うため(53°なら幅比1.28で非対称を保ったまま差15%)。
  **教訓: 小さいアイコンは「手で座標を置く」より「正しい幾何から生成する」方が速く正確**。半径・間隔・クリアランスは制約充足の探索で決めた。麻雀牌/花札も検討したが、**絵柄が記号の本体である図案は線画に落とすと判別不能**になるため却下(サイコロは輪郭も中身も単純で小サイズに耐える)。
- v4.17 **【実バグ・実データで発覚】振替カードの発火条件が「収入が1件でも着弾したか」だった**(ユーザーがバックアップを添付して指摘)。実データで**8/3の割戻金¥7,516が「給料が来た」と判定され、給料日(8/14)の10日前に「今月の振替がまだです あと¥36,808」が出ていた**——所持金¥33,016では物理的に不可能な催促。
  **`mainIncomeLanded(mk)`=その月の収入の過半が着弾したか、で判定する**ように修正。給与は当月収入の大半を占めるので臨時収入では発火せず、給料が入った瞬間に発火する。**カテゴリ名に依存しない**のが利点(「給与」という文字列に頼らない)。ふるさと納税カード(`furusatoDue`)の同じ判定も差し替え。ウィジェットもv9で追随。
  実データ検証: 8/3〜8/13は非表示、**8/14(給料着弾)で表示**、所持金も¥17,216→¥317,116。不変量テストに【10】を追加(invariants 55→59項目)。
  **教訓: 「収入が入った」を`some()`で見ない**。臨時収入・返金・少額の入金は日常的にあり、それを給料着弾と誤認すると**実行不可能な催促**を出すことになる。金額の比率で見る。
  ※参考: バックアップで確認した実データの状態——所持金¥33,016(ソニー銀行¥28,212/現金¥3,991/PayPay¥343/Suica¥470)、軍資金¥42,064、貯金¥40,000、8月可処分¥133,617、振替目標 貯金¥36,808。軍資金の月別合計¥10,123=残高−開始残高で整合。
- v4.16 ①**「先取り」→「振替」に統一**(ユーザー指摘: 意味が分からない)。バンド名・カード文言(「今月の振替がまだです」)・投資のロードマップ・漏れ係数の説明文まで画面上の表記を全て置換。**行為の名前ではなく何をするかで呼ぶ**という今回の一連の方針の締め。
  ②**固定費ごとに財布を指定できるように(ユーザー指摘)**。`S.fixed[].wal`。実態として家賃・光熱費はソニー銀行、駐輪だけSuica のように分かれるため、テンプレに持たせて`genFixedEntries`が生成エントリへ引き継ぐ。**財布を変えたら過去の生成分も付け替える**(引き落とし口座を変えたら過去も同じ口座だった、が実態に近い)。財布が1つのうちはセレクタを出さない。
  ③参考: **先取りカードが「急に出た」のは設計通り**——v3.91の条件(今月+収入着弾済み+未達)で、給料が着弾した瞬間に現れる。
- v4.15 ①**【実バグ】集計開始日(S.bank.switch)を撤去**(ユーザー指摘)。v4.12で月別表示にしたのに`bankGameNet()`だけが開始日以降しか数えておらず、**月別の合計と残高が一致しない状態**だった(実データで確認: 月別は7月の遊技+¥10,123を数えるが残高は+¥0)。遊技は全期間を数えるようにし、**開始時の残高は「記録開始前の残高」と定義し直した**。表示も「集計開始日(…)以降の勝ち負け」→「遊技の勝ち負け」へ。編集からも撤去。
  **⚠️ この変更で軍資金残高が変わる**——開始日より前に記録した遊技分だけ増える。ユーザーの実データでは +¥10,123(¥40,000→¥50,123)。開始時の残高を「開始日時点の残高」として入れていた場合は、その分を差し引いて入れ直す必要がある。
  ②**資産タブの軍資金の数字を赤くしない**(ユーザー指摘: 借金しているように見える)。**残高が少ないのはマイナスではない**。赤にするのは`bBal<0`のときだけ。警告はバーの色とバー下の行(⚠残りわずか / 上限超え / 天井)が担う。軍資金画面(v4.09)と同じ文法に統一。
  ③**総資産カードの3分割内訳(所持金/軍資金/貯金)を撤去**——すぐ下に各ポットのカードが全て並んでいるので二重表記だった。
- v4.14 **所持金を財布に分割(ユーザー設計)**。現金/口座/PayPay/Suica のように**使い分けの違う入れ物**を分ける。貯金の箱と同じ構造(`S.wallet.pots=[{id,name,seed}]`)。
  **記帳時に「どの財布から出すか」を選ぶ**(§6.5で先送りにしていた設計をここで実装)。`entry.wal`、貯金/軍資金txnの`walId`、財布同士の移動`S.wallet.txns`(両側adjust)。**財布が1つのときはセレクタを出さない**ので摩擦は増えない。
  **移行は非破壊**: 財布が無ければ既定の1つを作り旧`S.wallet.seed`を起点残高に引き継ぐ。既存のentry/txnには先頭の財布のidを冪等に書き込む(v4.04と同じ手法)。**実測で移行前後の所持金が同額であることを確認**。
  **総額の定義を変えないのが設計の要**——`walletBalance()`は引数省略で従来通り総額を返すので、**nowCaps(所持金基準の実効枠)・資産タブ・月末見込みなど上位の機能は全て無変更**で動く。
  所持金画面は**累計の内訳カード(貯金へ/軍資金へ…の全期間累計)を撤去し、財布ごとの残高+構成比グラフ+編集/調整/削除**に置換(ユーザー指摘: 累計はどの判断も変えない)。調整は**実残高を入力すると差額を自動で補正**する方式。
  **ウィジェットもv8で追随必須**——`wallet.seed`を直接読んでいたため、未更新だと所持金がズレて実効枠まで狂う。parityテストに財布分割ケースを追加。
  不変量テストに【9】(移行後も同額/財布別合計=総額/財布間移動で総額不変/支出は指定財布だけ減る)を追加。invariants は45→54項目。
- v4.13 所持金画面と下バーの整理(ユーザー指摘)。①**内訳の出ていく方向を赤に**——金額に色を付ける体系(v3.92)と揃える。ゼロは「—」なので無彩色のまま。②**末尾の「お金の移動は各画面から行います」を撤去**——下バーから直接振替できるようになったので案内自体が不要。
  ③**下バーに「振替」を追加**(`SUBNAV_TRANSFER = wallet/bank/savings`)。**常時見えて親指が届く位置にあるので、本文中のボタンより確実に速い**。これに伴い**貯金・軍資金の本文中の「振替する」ボタンは撤去**(重複)。軍資金の「上限超えを貯金へ」は金額を前埋めする別物なので残置。**投資は買付が別建て(transferAccountsに含まれない)ので対象外**——ここに振替を出すと押せない行き先を見せることになる。
  下バーの構造は`.subnav`をflexにして`[‹ 戻る | 振替]`の2分割。振替が無い画面は従来通り1つ。
- v4.12 **軍資金に月別の増減内訳を追加(ユーザー指摘)**。残高を動かすのは5つ(遊技の勝ち負け/補充/回収/所持金へ戻し/補正)だが、**記録セクションに出ていたのは下4つだけで、一番大きく動かす遊技の勝ち負けが明細に存在しなかった**——残高が減っていても理由が読めない状態だった。
  **明細を月で区切るのではなく「何で増減したか」を月ごとに分解する**のが正しい形。1行目=その月の残高変化、2行目=内訳(遊技/補充/回収/所持金へ/補正)。補充が枠を超えた月はその旨も出す。**「負けた月にいくら補充したか」が一行で読める=漏れ係数の材料そのもの**。
  `bankMonthly()`は`S.entries`の遊技(src=slot/mahjong)と`S.bank.txns`を月別に集計。**`bankGameNet()`と違い集計開始日でフィルタしない**(月別に見るのが目的なので全期間)。不変量テストに【8】(月別netの合計=残高−開始残高 / 遊技以外の支出が混入しない / 新しい順)を追加。invariants は42→45項目。
  **補充枠のリセットは暦月(1日)で確定**——給料日基準にすべきかを検討したが、ユーザー指摘の通り**年間の補充総量は変わらず、サイクル内のどこに落ちるかが変わるだけ**なので暦月で問題ない。
- v4.11 **軍資金の補充に月あたりの枠を設ける(ユーザー設計)**。`S.bank.refillCap`(既定¥30,000/月)。**枠内は黙って通し、超えた分だけ警告**する。v4.10の全件警告は月初の計画的な投入でも出るため必ず読み飛ばされる——**毎回出る警告は警告ではない**。
  **判定は「その暦月に軍資金へ入った合計(topup)」だけを見る**。ユーザーの指摘通り`貯金→所持金→軍資金`と迂回しても最後は必ずtopupになるので、**経路で抜けられない**。箱→軍資金の直行(withdraw+topupの組)も同じ枠を消費する。逆流(軍資金→貯金=回収)と補正(adjust)は枠に無関係。
  枠は軍資金画面の[編集]で変更可、集計カードに「今月の補充 ¥X / ¥30,000」を常設(**使い切る前に見えていないと意味がない**ので、超過してから知らせる形にはしない)。
  不変量テストに【7】補充枠(先月分を数えない/迂回でも消費/直行でも消費/回収と補正は無関係)を追加。invariants は35→42項目。
- v4.10 **軍資金への振替は出所を問わず全て警告する(ユーザー指摘)**。v4.05は貯金からの補充だけを見ていたが、**実際に起きるのは所持金からの補充**——負けた分をその場の手元金で埋めるのが現実の経路。**出所で漏れかどうかは決まらない。軍資金へ金が入る=遊技の負けを外から埋める、という一点だけが本質**。文面のみ出所に合わせ、所持金からなら「生活費を削って遊技に回す行為」、箱からなら「目的を決めて取り分けた金を崩す行為」と何を削っているのかを名指しする。現在の軍資金残高も併記。逆方向(軍資金→貯金=回収)は当然警告しない。
  **残る論点(未着手)**: 月初の計画的な資金投入でも警告が出るため、慣れて読み飛ばす恐れがある。精緻化するなら「**集計開始日以降2回目以降の入金だけ警告**」——ユーザー自身の約束(「尽きたら来月まで補充しない」)がそのまま判定式になる。1回目=計画、2回目以降=補充。実機で読み飛ばしが起きるようなら導入する。
- v4.09 軍資金画面を貯金画面と同じ構造へ(ユーザー指摘)。①**説明文4行を撤去**。②**「残りわずか」の独立警告カードを廃し、バー直下の行に表示**——状態はグラフのそばで語る。同じ行は上限超え時に「上限超え +¥X」、通常時は「¥0」に切り替わる(1行が3状態を担う)。③**設定3つ(上限/集計開始日/開始時の残高)を[編集]に畳む**——常設カード2枚を占有していたが、どれも滅多に触らない。貯金の箱と同じ`編集/調整`の小ボタン様式。④振替を全幅ボタンで下部へ。カード5枚→3枚に。
  **【注意】「尽きたら来月まで補充しない約束」「貯金から補充=20年来の漏れ」の文言はここから消えたが、実効的な歯止めは失っていない**——v4.05で振替側へ移した**箱→軍資金の警告confirm**が、実際に補充しようとした瞬間に発火する。受動的な貼り紙より行動の瞬間に出る方が効く。
- v4.08 貯金画面の「軍資金からの振替 累計 / 所持金からの振替 累計」を撤去(ユーザー指摘)。**貯金側で「どこから来たか」を出しても行動が変わらない**。軍資金からの回収額はやる気に効くが、それは**軍資金画面の「貯金に回した累計」が既に担っている**ので、同じ数字を2箇所に置かない。未使用になった`savingsDeposits()`も撤去(v3.55)。
- v4.07 貯金画面の操作配置を整理(ユーザー指摘)。①**調整を各箱の小ボタンへ**——固定費の「編集/停止」と同じ様式に揃え、箱ごとに`編集 / 調整 / 削除`を右寄せで置く。**どの箱を調整するかがdata-vで確定する**ので、prompt で箱を選ばせる段階が消えた。②**カード全面タップを廃止**——押せる場所がボタンとして見えるようになった。③「＋箱を追加」と「振替する」を**全幅ボタンで縦に**並べ、**箱の操作を一通り見せた後に振替**という動線に。④調整の説明文を撤去。
- v4.06 **資産タブのサブラベルと説明文を撤去(ユーザー指摘)**。所持金の「生活口座」「次の給料日まで、変動消費と娯楽に使える上限です。」、軍資金の「緩衝プール」を削除。**同じ説明を毎日見せる必要はない**——意味は各ポットの詳細画面が持てばよく、一覧カードは残高だけで足りる。詳細画面側の文からも「緩衝プール」「生活口座」という言い回しは外した(意味は残す)。予定が払えない場合の警告は状態の通知なので残置。
  **「所持金」の改名は据え置き**(検討済み)。他の3つ(軍資金/貯金/投資)は目的名だが所持金だけは「まだどこにも割り当てていない金」なので、目的で名付けると必ず実態とズレる。「生活費」は娯楽・先取りも出るため不正確。改名するなら「手元」が最有力だが、現行で破綻していないため変更しない。
- v4.05 **振替を記帳画面へ完全移植(ユーザー設計)**。記帳のタイプを「支出/収入/**振替**」の3つにし、金を動かす行為の入口を1箇所へ集約。貯金・軍資金の各画面は**見る台帳に純化**し、残す操作は**調整(と軍資金の開始時残高)だけ**。
  **振替はS.entriesに一切書かない**——収支ではなく財布の間の移動なので、当月収支は動かない(v3.80の定義通り)。画面にも「振替は収支に含まれません」と明記。保存は`doTransfer`経由で各台帳のtxnへ落ちる。
  **編集中(editEntryId!==null)はタイプに振替を出さない**——既存の収支エントリを振替に化けさせないため。
  **入口は複数・実装は1つ**: `openTransfer(from,to,backScreen,amount)`で貯金画面/軍資金画面から記帳の振替モードへ飛ばし、完了後は元の画面へ戻る。**台帳から操作できないと、残高を見て「移そう」と思った瞬間に詰まる**(v3.67の教訓)ので導線は残した。軍資金の「上限超えを貯金へ」は金額を前埋めして開く。
  **【落としてはいけなかったもの】旧軍資金画面(v3.82)の「貯金から補充=負けを外の金で埋める行為=20年来の漏れ」警告を振替側へ移植済み**。箱→軍資金の振替時にのみ発火。入口を集約しても**アプリの存在理由に関わる警告は絶対に落とさない**。
  旧`topup`/`sweep`/`sweepover`ハンドラは死コードなので撤去(v3.55)。
  **【ミス1件・actions.testが検出】旧ハンドラの一括削除で、直前に挿入した`gotr`/`gotrover`ごと巻き込んで消していた**。UIは正しく描かれ、押しても何も起きない状態(=v3.95と同じ最も気付きにくい壊れ方)。**挿入と削除を同じ領域で連続して行うと、後の削除が前の挿入を巻き込む**——範囲削除の境界は挿入後のテキストで取り直すこと。
- v4.04 ①**「から/へ」→「振替元/振替先」**(ユーザー指摘・見栄えの整合)。
  ②**【設計ミスの修正】既定箱を削除できない問題**。v4.00で`"main"`を特別扱い(potId無しtxnの受け皿)にしたため、**引越しが終わって引越し用の箱が不要になっても消せない**状態だった。指摘の通り設計として間違い。
  **解決**: applyDataで**potIdの無いtxn(とsweep)に先頭の箱のidを冪等に書き込む**。これで「potId無し=既定箱」という暗黙の規則が消え、**全ての箱が対等**になる。意味は変えていない(今まで既定箱として集計していた帰属を明示するだけ)ので実質無害な移行。以後どの箱も削除できる(最後の1つを除く)。「既定」ラベルも撤去。
  **削除時の引き取り先はユーザーが選ぶ**——勝手に特定の箱へ寄せると目的の違う金が混ざり、箱を分けた意味が消えるため。残高・履歴とも移すので記録は失わない。
  不変量テストに【6】箱の削除(potId移行の冪等性・削除しても総額不変・最後の1つは残る)を追加。invariants は31→35項目。
- v4.03 v4.02の実機フィードバック2件(ユーザー指摘)。
  ①**振替を左右2カラムへ**。横並びチップ+`flex-wrap`だと**項目名の長さで幅がバラつき折り返しも起きて不揃いに見えた**。同じ並びの列を「から」「へ」で対にし、各ボタンを列幅いっぱい(名前と残高の2段)に。振替元に選んだ財布は「へ」側で**同じ高さの空ブロックに置き換える**ことで左右の行が対応し続ける(単に消すと行がずれる)。財布名から「(貯金)」の接尾辞を撤去——列見出しで文脈が付くので冗長。
  ②**「調整」を振替カードから箱セクションへ移設**。**振替は金を動かす操作、調整は実残高とのズレを埋める記録**でまったく別の行為なのに同居していた。調整は箱に対する操作なので「＋箱を追加」の隣が定位置。
- v4.02 v4.01の実機フィードバック4件(全てユーザー指摘)。
  ①**振替をprompt連打からタップ選択へ**。財布(所持金・軍資金・各箱)をチップで並べ「どこから→どこへ」を押して選び、金額だけ入力する。選択状態は`UI.trFrom`/`UI.trTo`/`UI.trAmt`。振替元に選んだ財布は振替先の候補から自動で外れる。**prompt("1"などの番号入力は直感的でない**というのが指摘の核心。
  ②**旧goals(順次切り替わる梯子)を廃止し、目標を箱ごとに持つ**。`pot.target`。**「どの残高で目標を測るか」という問題(v4.00の最重要判断)が消滅する**——各箱が自分の残高で自分の目標を測るだけになるため。`ladderBalance()`/`savingsCurGoal()`は撤去。投資スタートまでの距離は`potsShortfall()`=**目標を持つ全箱の不足合計**。影響3画面(資産タブ/投資のロードマップ/AI監査プロンプト)を箱ベースへ書き換え。**移行**: 既定箱に目標が無ければ旧goalsの進行中のものから一度だけ引き継ぐ(実測で¥500,000が引き継がれることを確認)。旧goalsの初期投入は廃止したが、フィールド自体はバックアップ往復のためapplyDataで保持。
  ③**構成比グラフ**。箱が2つ以上かつ残高>0のときだけ、合計に対する各箱の比率を1本の帯+凡例で示す。箱ごとのカードには目標進捗バー。
  ④**「スイープ」表現を振替に統一**。軍資金画面のラベル(topup/sweep/return)も全て「振替(◯◯から/へ)」へ。行為の名前ではなく**どこからどこへ動いたか**を書く。
  **【ミス1件】`class="inp"`は存在しない**(正しくは`input mono r`)。金額欄が無スタイルになる状態を実機投入前に修正
- v4.01 **資金移動を「振替」1つに統一(ユーザー設計)**。従来は所持金・軍資金・貯金の間の移動が`topup`/`return`/`sweep`/`deposit`/`withdraw`と**5つの別名で3画面に散在**し、「先取り入金」のように**行為の名前が意味を伝えていなかった**。財布(所持金・軍資金・各貯金箱)を並べて from→to を選ぶ形へ一本化。
  **データ構造は変えない**——`doTransfer(from,to,amt)`が既存のtxn種別へ機械的にマップするだけなので過去の記録は無変換で生きる。対応表: 所持金→軍資金=topup / 軍資金→所持金=return / 所持金→箱=deposit(potId) / 箱→所持金=withdraw(potId) / 軍資金→箱=sweep(potId・**v4.01でsweepもpotIdを持てるように**) / **箱→軍資金=withdraw+topupの組**(所持金への影響が相殺されてゼロになる) / 箱→箱=両側adjust(depositとwithdrawだと所持金が二重に動く)。
  **ふるさと納税の箱紐づけ(v4.00)は撤去**——どの箱から出すかは**支出の記帳時に選ぶ**方が自然というユーザー指摘。`furusatoDue.covered`は貯金全額を見るだけに戻した。
  **不変量テストに「振替は総資産を変えない」を常設化**(8経路×資産保存則)。invariants.test は22→31項目。
- v4.00 **貯金を複数の箱(pot)に分ける(ユーザー設計)**。実口座を分けられる(みんなの銀行)ので、目的の違う金——11月の引越し資金と12月のふるさと納税——をアプリ側でも混ぜない。
  **データ構造**: `S.savings.pots=[{id,name}]`。既定箱は`"main"`で必ず先頭に存在。**txnの`potId`が無いものは全てmain扱い**なので既存データは無変換で動く(非破壊移行)。軍資金からのスイープ(bankSaved)もmainに入る。`savingsBalance(potId)`は**引数省略で全箱合計・指定でその箱**(既存の総資産表示はそのまま動く)。
  **【最重要の設計判断】梯子(引越し→生活防衛費→投資スタート)の進捗は`ladderBalance()`=mainの残高だけで測る**。ふるさと納税用に積んだ金は引越しには使えないので、梯子に算入すると**「11月に達成と出たのに実際は足りない」という最悪の誤認**が起きる。対象4箇所(viewAssets/viewSavings/viewInvestのロードマップ/AI監査プロンプト)を全て`ladderBalance()`へ。**一方 securedSplit(先取り実績)は全箱合計のまま**——あちらは「生活口座から出した額」の計測なので、どの箱に入れたかは無関係。
  **ふるさと納税との接続**: `S.furusato.potId`で箱を紐づけ、`furusatoDue.covered`はその箱の残高を見る(未紐づけなら総額)。貯金画面の箱行の「ふるさと」チップでトグル。
  **UI**: 箱が1つのときは従来と全く同じ見た目(内訳も箱選択も出ない=摩擦を増やさない)。2つ以上で初めて内訳・箱選択・「箱の間で移す」が現れる。**箱間の移動は両側をadjustで立てる**——withdraw+depositにすると所持金(walletBalance)が二重に動くため。箱の削除は残高と履歴をmainへ寄せる(記録を失わない)。
  検証: 引越し資金12万+ふるさと納税5.2万のケースで、合計17.2万・梯子は12万(30%)・12月の控除ゼロ・消費枠60,130維持を確認。ウィジェットにも移植(v6)しparityテストに箱ケースを追加。
  **【作業中のミス1件】`replace(...,1)`がviewSavingsではなくviewBankの同じlistitemパターンに当たった**——`pots is not defined`で軍資金画面が落ちる状態を描画スイープが検出。**同一パターンが複数のviewに散在しているので、置換対象は必ず一意な文脈込みで指定すること**
- v3.99 **【v3.98の設計修正】ふるさと納税の控除は「貯金で賄えない分」だけにする(ユーザー設計)**。
  v3.98は残枠を丸ごと可処分から引いていたが、**実際の捻出は貯金(ストック)から行う運用**なので、12月のフローからも同額を確保するのは二重取りだった(寄付前の期間だけ消費枠が半減する)。**控除 = max(0, 残枠 − savingsBalance())**。
  ・貯金が足りていれば控除ゼロ=12月の生活費は一切削られない ・積立が不足していれば差額だけフローが締まる(**サボると12月に締まる、という正しいフィードバックになる**) ・**「余ったら貯金に回す」は自動的に成立**する。実額だけ引き出せば残りは元々貯金に居るので、箱の移し替えという操作自体が発生しない
  実測(収入30万/固定費179,740/限度額52,000): 貯金12万→控除0で消費枠60,130のまま / 貯金3万→控除22,000で消費枠49,130 / 貯金0→控除52,000で消費枠34,130。ウィジェットにも移植(v5・savingsBalanceも移植)、parityテストに充足/不足の両ケースを追加。
  **【重要な事実確認】貯金は「複数の箱」ではない**。`savingsBalance()`は**単一残高**(スイープ+入金−引出+調整)で、`goals`は**順番に切り替わる目標**にすぎない(貯金画面の説明文にも「同じ箱で目標だけが切り替わる」と明記)。txnsにgoalIdは無く、目標ごとの残高配分も箱間の移動も存在しない。**「◯◯用の箱に積む」「余りを別の箱にスライド」は現状できない**——ただし上記の設計ならその必要が無い。将来もし本当に箱を分けるなら、txnsへのgoalId付与とsavingsBalanceの目標別集計という構造変更になる(=大改修。要求が出るまで着手しない)。
  **却下: 毎月一定額を「ふるさと納税」として記帳する案**——寄付していない月に支出を計上することになり「記帳は事実」の原則に反する。当月収支も実態とズレる。
- v3.98 **ふるさと納税を12月の最優先枠にする(ユーザー設計)**。限度額は年内に使わないと消える権利で12月が最後の機会なので、「余ったらやる」ではなく**可処分から真っ先に確保**し、他の枠(消費・娯楽・貯金・投資)はその残りで決まる。
  **実装は`disposableBase`に控除を1行入れるだけ**——月枠の%指定も`preSplit`の按分も全てここを基準にしているので、1箇所で全部が自動的に縮む。寄付を記帳するとdoneが増えleftが減り控除が緩んで他の枠が戻る(自己調整)。表示は**先取りカードの最上段**(色はamber=消費でも貯蓄でもない期限付きの権利行使)。出す条件は先取り(v3.91)と完全に同じ: **12月 かつ その年の枠 かつ (収入着弾済み or 一部寄付済み) かつ 残枠あり**。記帳規約はv3.38のまま(店名「ふるさと納税」固定・分類=承認なのでバー対象外)。
  検証(限度額8万/収入30万/固定費6万): 11月=控除なし可処分24万 → 12月給料着弾で**可処分16万・貯金9.8万→4.2万** → 3万寄付で可処分19万に戻る → 使い切りで控除消滅。ウィジェットにも移植済み(v4)で12月ケースの一致を parity テストに追加。
  **【自作バグ1件・ハーネスが検出】`isNetEntry`はviewMain/viewCatDrillのローカル定数でグローバルではない**。トップレベルの`furusatoDue`から参照して即ReferenceError→disposableBase経由で全画面が落ちる状態だった。**同名のローカルヘルパが複数のview内に重複定義されている構造**なので、トップレベル関数から呼ばないこと(判定を自前で持つ)。v3.91の先取り条件も同じ式を使っているがあちらはviewMain内なので正常
- v3.97 **アプリ全体の客観チェック(ユーザー依頼)+検出したバグ1件の修正**。
  **【実バグ】`S.furusato`(ふるさと納税の限度額)がapplyDataのホワイトリストから漏れていた**——applyDataは唯一の正規復元経路なので、**クラウド復元・インポート・スナップ復元・他端末からのpull の全経路で限度額が消える**状態だった。同一セッション中はS上の値が生き残るため実機では気付けない。**v3.13(openAddOnLaunch)と全く同じ事故が2度目**。修正済み。
  **テストハーネスを`tests/`として常設化**(node単体で動く・外部依存なしでオフライン環境でも実行可)。`harness.js`がDOM/localStorage/navigatorのスタブを作り、index.htmlの実物のインラインJSをvm上で起動する。以後`vm.runInContext`でrender/applyData/mjtSettle/nowCaps等を直接呼べる。4本の内訳:
  - `render.test.js` **全画面×5データシナリオ(空/通常/境界0除算・マイナス/卓なし/未来日)の描画スイープ=115通過0失敗**。v3.58型(変数定義の削除漏れ→描画例外)を捕まえる
  - `invariants.test.js` 麻雀精算の不変量(Σ調整=0 / Σ場代移転後=生 / Σ支払=−店総額 を4ルールで)、可処分・nowCaps・preSplit・所持金の恒等式、保存→復元ラウンドトリップ、**§4=S直下の全キーがapplyDataに載っているかの自動照合**(今回のfurusatoはこれが検出)。22通過0失敗
  - `actions.test.js` **死んだボタンの静的検出**——出力される`data-XX="値"`に対応するハンドラ分岐が無いものを洗う。v3.63(wfapply)/v3.65(gcap)/v3.95(subback)と同型の事故用。※値の運搬用属性(data-v/bi/sf/wn/in)とbindMjTableの明示スキップ配列は誤検出なので無視してよい
  - `widget-parity.test.js` 本体とtally-widget.jsの計算一致(disposableBase/walletBalance/nowCaps)。**予算・所持金まわりを触ったら必ずこれを走らせる**
  **今後の運用: 機能を足したら最低でも render.test と invariants.test を走らせる**。特に「S直下に新フィールドを足したら」§4が自動で拾う(v3.13の教訓を規律から機械へ移した)
- v3.96 v3.95下バーの実機フィードバック3件(全てユーザー指摘)。
  ①**【重大バグ】下バーが無反応だった**。原因=**bind関数16種が`document.querySelectorAll("[data-a]")`で#appに限定せず全文書を走査してonclickを上書きする**ため、`data-a="subback"`が各画面のハンドラに奪われ、どの分岐にも当たらず沈黙していた。**#navbarは#appの外にあるので、bind関数と衝突しない属性名`data-nav`へ変更**して解消。静的検査で全走査属性(data-a/bu/mt/lk/iv/wa/ba/sa/wn/wd/fa/in/bi/bseg/bstart/sf)と交差しないことを確認。**教訓: #navbar内に置く要素にbind関数の走査属性を使わない**——UIは正しく描かれ、押しても何も起きないという最も気付きにくい壊れ方をする
  ②**ヘッダーの戻るとの二重表示を解消**: 下バーがある画面ではヘッダーの戻るを`visibility:hidden`で隠す(`display:none`だと右端44pxスペーサーとの釣り合いが崩れて見出しがずれる)。**要素は残すのでバインドが生き、click()も発火する——遷移先を別途書かずに済ませるにはボタン自体が必要なので削除はしない**。実装は_renderBodyで`findHdrBack(app)`が特定して`_hdrBack`に保持し、goBackViaHeaderはそれをclick()するだけ(毎回の再探索も不要に)。エラー画面では_hdrBack=nullにして古いDOMを掴み続けない
  ③**バーの様式を.tabs流用から専用の.subnavへ**: タブ様式(20px角丸・縦積みアイコン+ラベル)だと「タブが1個しかない」ように見えて浮いていた。**横並び1行・角丸14px・低い背**にしてナビゲーションの帯として読ませる
- v3.95 ①**navbarの無い画面に「戻る」バーを新設(ユーザー設計・案B)**: 従来は戻るがヘッダー左上にしか無く、下へスクロールすると一度上まで戻らないと出られなかった。しかもiPhoneの上端は片手で届かないので「スクロール+持ち替え」の二重苦。**#navbarは既に#appの兄弟でフロー最下段・--navh実測注入・フェード帯・safe-area対応が済んでいるので、非表示にしていた枠にバーを入れるだけ**で済んだ(新しい浮遊物は作らない=v3.04の方針維持)。保存インジケーターもここへ同居させ、v3.94の「本文末尾へ追記」は廃止。**戻る先は書かない——`goBackViaHeader()`がヘッダーの戻るボタンをclick()する**(属性名はdata-a/wa/sa/mt/lk/iv/fa/bu/baと9種バラバラだが全て`.navbtn`で先頭が「‹」。ここに遷移先を書くと同じ挙動が2箇所に増える=v3.63の教訓)。**除外はmain(タブバー)/mjsheet(共有紙面)/mjplay(既にstickyヘッダーに‹があり下端はstickyキーパッドが使用・保存表示のみ本文末尾に残置)**。TAB_ICONS.back(シェブロン)を新設
  ②**スコア表の削除を詳細画面から一覧(mjtlist)へ移設(ユーザー指摘)**: 清算画面は対局中・清算中に開いている画面なので、**進行中の卓が消える事故の入口をそこに置かない**。一覧側は**v2.89の削除モード方式**を踏襲(ヘッダー「削除」トグル→各行に削除チップ→1件で自動解除、モード中は行タップの開くを無効化・＋新規も隠す)。確認ダイアログは**卓の中身を読み上げる**(日付/同卓者/半荘数)ので取り違えにくい。**遊技タブの収支記録(fromTable由来のmjSessions)は削除されない**——消えるのは半荘ごとのスコアのみで、金の記録は残る(その旨をダイアログにも明記)。削除モードはエフェメラル(v3.00の思想・他画面から一覧へ入るたび解除)で「前回の削除モードが生きていた」事故を構造的に排除。bindMjTableの旧delハンドラは死コードなので撤去(v3.55)
- v3.94 **保存インジケーターを全画面へ・文言統一(ユーザー指摘)**: 「作業中が一番不安なのに、画面によって出たり出なかったり書式が違うと時刻を出す意味自体が無くなる」。①文言をnavbar版と同じ短形`✓ 自動保存 HH:MM:SS ・ vX.XX`に統一(旧saveIndicatorHtmlの長文は廃止) ②**挿入を_renderBodyに一元化**——`_renderScreen`成功後に`app.insertAdjacentHTML("beforeend", ...)`で追記し、view側の個別呼び出し8箇所を撤去。**規律でなく構造で担保**(view側に書く方式だったからスコア表など書き忘れの画面が生まれた)。**教訓: 全画面に出すべきものはview関数に配らずrender側で1箇所から差す**。除外はmjsheetのみ(共有される紙面に余計なものを焼き込まない)。`.savets`のライブ更新機構は追記要素も自動で拾う。**今後view関数内でsaveIndicatorHtml()を呼ばないこと(二重表示になる)**
- v3.93 navbarの保存インジケーターの「通信不要」を**バージョン表記(v3.93)**へ(ユーザー要望)。更新が反映されたかを画面を移らずに確認できる方が実用的。オフライン保証の断言は saveIndicatorHtml(バックアップ画面等)に残っているのでv2.92の方針は失われていない
- **tally-widget.js v3(Scriptable・リポジトリ外)**: 本体と数字が食い違っていた重大バグを修正。①**上限が月枠のままで実効枠(v3.85)を知らなかった**+`parseFloat(B[cat])`が複合指定"70000|50%"を70000と誤読 → 変動消費の残りが¥68,479(本体は¥14,979)と**53,500円ズレ**ていた。`nowCaps`/`walletBalance`/`disposableBase`/`budgetAmt`を移植して解消 ②**娯楽の集計ミス**——本体の条件は`(cat!=="消費" || e.fixedId==null)`で固定費を除外するのは消費だけなのに、ウィジェットは娯楽にも除外をかけていた ③色連動(下記v3.92の規約に追従)。**検証: 本体の実物の関数をvmで抽出して突き合わせ4項目一致**(disposableBase/walletBalance/nowCaps.varc/nowCaps.amuse)+所持金拘束経路で実画面値(¥16,500/¥14,979)を再現。**教訓: ウィジェットは本体の計算規則の複製なので、予算・所持金まわりを触ったら必ずウィジェットの追随を確認する**(v3.20以降ずっと放置されていた)
- v3.92 **金額の色をバー連動へ(ユーザー指摘・今回の結論)**: 変動消費=赤 / 食費内枠=薄い赤(opacity .55) / 娯楽=琥珀 / 貯金=teal。**このアプリは金に色を付ける体系(緑=プラス/赤=消費/橙=中間)なので、一番大きい金額だけ無彩色の白は体系外で浮く**というのがユーザーの論拠で、これが正しかった。超過時も同じ分類色——区別は語(残り/超過)とバーが満杯になる形が担う(v3.35と同じ考え方)。語(残り/あと/超過)とラベルはmutedのまま=**色が付くのは金額だけ**。
  **【規約の失効】v3.49の「色はバーに一元化、金額は通常色」は予算バーについては失効**。あれは内訳画面で7個の色数字が積み上がる密度の話で、v3.49自身が「予算バー前例は視野に1個で密度が別物」と但し書きしていた。v3.88でそれを拡大解釈して白にしたのが遠回りの原因
- v3.91 ①**先取りを常設から催促式へ(ユーザー設計)**: 先取りは月に一度やれば終わる仕事なので、済んだ後も居座るのは計器でなく雑音。出す条件=**表示中が今月 かつ (当月の収入が着弾済み or 既に一部実行済み) かつ 未達**。達成した瞬間にカードごと消える=「終わった」ことが最も強い報酬になるので「達成」表示は不要になった。検証9/9(予定収入だけの月初は非出/一部実行は出続ける/完了で消える/過去月は非出)。**プッシュ通知は§8で却下済み——画面上の出し分けが同じ役割を果たす**(オフライン憲法とも整合)。副作用として投資行の「押す予定のないボタンが常駐」問題も解消(按分比の正規の入口は予算設定画面v3.75なのでv3.67の教訓に抵触しない) ②ラベル(変動消費/今月の娯楽/貯金)をmutedへ。**※この時点の「白は行に1つだけ」規約はv3.92で上書きされた**(白そのものを使わなくなったため)——ラベルがmutedである点だけ生きている
- v3.90 **「使う」と「移す」を別カードへ(ユーザー指摘)**: 消費・娯楽=使ってよい額 / 貯金・投資=移すべき額。**行為の種類が違うものを同じカードに積むと、同じ位置・同じ書式のせいで同じものに見え、色だけで区別しようとして破綻する**。band「予算」と band「先取り」の2枚に分割。「先取り」は v3.61以来の内部語彙だが「先取り貯蓄」として一般に通じるため画面語彙に昇格(v3.67で却下した「滝」は開発上の内輪呼びなので別件)
- v3.89 ジャンル内枠のバーを灰(--muted)から**薄い赤(--red + opacity .45)**へ。親が赤なのに子が灰色で、子の方が目立っていた。超過時は不透明の赤に切り替わり親と同じ強さになる
- v3.88 **予算バーの数字に1語のラベルを付ける(ユーザー指摘「パッと見、意味が伝わらない」)**: 消費・娯楽=「残り」、貯金・投資=「あと」、超過=「超過 ¥X」(v3.87の`−¥X`から復帰)、内枠も同文法。**v3.86で「残り」の語を入れたのにv3.87で主役を右上へ移した時に語を落としたのが原因**で、無名の大きい数字が2種類(あと使える/あと入れる)並んでいた。内訳画面は既に「残り ¥X / 超過 ¥X」だったので、**メイン画面だけが文法から外れていた**
- v3.87 **残りを主役に(ユーザー設計・予算バーの最終形)**: **使った額は振り返りの数字、残りは意思決定の数字**。買い物の前に見るのは後者なので主役を入れ替えた。
  **確定した文法(蒸し返さない)**:
  - 左: ラベル + 小さく「使った / 上限」(10px muted)
  - 右: **残り(17px 太字)** + 「›」。超過時は **−¥X** と符号で示し分類色
  - 下: 細いバー(8px)のみ。**導線行は廃止**しカード全体をタップ可能に
  消費・娯楽・貯金・投資・ジャンル内枠すべて同一。変動消費ブロックは2行(本体+内枠)まで簡素化。
  v3.86でバー内に置いた残額はこの再設計で不要になった(主役の位置に出るため)
- v3.86 **残額をバーの中へ+表記の一貫性(ユーザー指摘)**: ①**「所持金に合わせた枠」の注記を撤去**——自分で設定した仕組みなので説明は不要で、シンプルさを損なっていた ②**残額をバーの内側(右寄せ)に配置**。空のバーは情報を持たないので、そこに数字を置けば行が1つ減り視認性も上がる。バー高を7px→18pxに ③**語彙を「残り」に統一**(貯金の「あと」→「残り」)。消費・娯楽・貯金・投資・ジャンル内枠・内訳画面の全バーで同一文法 ④**「予算オーバー」の帯を廃し「超過 ¥X」の金額表記へ**(語より数字。他のバーと同じ読み方になる) ⑤下段は導線のみ(内訳を見る / ○○へ › / N件)。**確定した文法(蒸し返さない)**: 上段=使った/上限、バー内=残り or 超過、下段=導線。全画面共通
- v3.85 **所持金が厳しい時は予算の上限そのものを書き換える(ユーザー設計・最終形)**: 別行で「いま使えるのは」を出すのでなく、**上限の数字自体を所持金基準に差し替える**。**実効枠 = 使った額 + 所持金×割合**(照合の式が肝心。これにより「残り」がそのまま「いま使える額」になる)。例: 使った1,521 + 所持金29,957×50% = 実効枠16,500、残り14,979。月枠を超えることはなく、月の後半でも同じ式で成立。縮小中は「残り ¥14,979 ・ 所持金に合わせた枠」と理由を添える(月枠は予算設定画面で確認できる)。**変動消費ブロックの数字は3個**(使った/実効枠/残り)まで削減。所持金が潤沢なら月枠のまま表示され注記も出ない。
  **既知の性質(仕様)**: 割合ベースなので支出1,000に対し残りは500しか減らない(残り25%がバッファとして負担を分け合う)。厳密な1:1の消化ではないが、「今ある金の何割まで」という規律としては一貫している。検証9/9
- v3.84 **予算バーの数字を削減+v3.83の二重計算バグ修正(ユーザー指摘「数字が多すぎる」)**: ①**【バグ】当座上限の「残り」が二重計算**だった——所持金は既に過去の支出を差し引いた後の額なので、そこから割り出した上限は「これから使える額」であり、更に spent を引くのは誤り。当該行を撤去し単独の値として表示 ②**下段を1行に統合**: 「残り ¥58,609 ・ いま ¥14,979 まで」+「内訳を見る」 ③**%表記を全撤去**(バーが視覚的に示しているので数字は冗長) ④**ジャンル内枠の「残り」を撤去**(上限−使ったで自明。超過という異常時のみ言葉にする)。結果、変動消費ブロックの数字は**8個+%2個 → 6個**(所持金が潤沢な月は5個)。**週次/月次の分割は不採用**: 「いま使えるのは」は既に**次の収入までの予算**そのもので、ユーザー自身が指摘した給料日跨ぎの問題も自動的に解決している(固定の週境界より優れる)
- v3.83 **「いま使っていい上限」の新設ほか3件(実機フィードバック)**: ①**月の枠と当座の上限を分離**(ユーザー設計・本質的な指摘)。月枠60,130でも所持金が29,957なら現実には使えない。`nowCaps(mk)`が**所持金×各枠の割合**で当座の上限を出し、予算バーに「いま使えるのは ¥14,979 まで」を破線区切りで併記。**割合は100%に引き伸ばさない**——消費50/娯楽25なら所持金の25%が自然なバッファとして残り、次の給料日までの不測の支出を吸収する(引き伸ばすとバッファが消えて所持金が尽きる)。月枠を超えることはなく(min)、所持金が潤沢で当座上限が月枠以上になる場合は併記しない(ノイズを出さない) ②**軍資金の残高を赤字にしない**。残高が少ないのはマイナスではないので、警告はバーと下の枠が担う。負の値のときだけ赤 ③**「リセット日」→「集計開始日」**。この日以降のセッション純額だけを軍資金に反映する集計起点であり、何もリセットされないため名前が実態と合っていなかった(機能自体は必要なので存置)。検証11/11
- v3.82 **所持金の意味付け・アラートの移設・軍資金の用語整理(実機フィードバック3件)**: ①**所持金カードから計算式を撤去**し「**次の給料日まで、変動消費と娯楽に使える上限です**」と意味を語る形へ(ユーザーの定義が正しい——所持金は残高であると同時に使える上限そのもの) ②**不足アラートを資産→家計簿タブへ移設**。支出を見直すのは家計簿画面なので、行動を促す警告はそこに出すべき。資産タブは残高を見る場所に徹する(v3.60の「主画面の予算カードは今いくら使ったかを見る場所」と同じ原則) ③**軍資金の用語を日本語に統一**: シード設定→開始時の残高 / スイープ→出金 / 補充→入金 / 調整→残高のズレを補正 / 天井→上限 / 切替日→リセット日。履歴のラベルも同様 ④**入出金は「どこから/どこへ」を先に聞く形へ**。入金は所持金から(通常)か貯金から(=漏れなので警告を維持)、出金は貯金へ(上限超えの回収)か所持金へ。**貯金からの入金時は貯金台帳にも引き出しを自動記録**して二重管理を防ぐ。v3.81で追加した「所持金へ戻す」ボタンは出金に統合して撤去。検証12/12
- v3.81 **所持金(論理残高)の新設(ユーザー構想・実装完了)**: 4つの独立プール(家計簿・貯金・投資・軍資金)を繋ぐ残高が無く、「予定の支出を払えるか」という**アプリが答えられなかった唯一の問い**を解消。**論理残高を採用**(実際の銀行残高ではない。カード払いの決済ズレは吸収しない=実口座とは差が出る旨を画面に明記)。**新たな記帳負担はゼロ**——既存の記録から全て算出できる:
  - 入: 収入 / 貯金の引き出し / 軍資金からの戻し / 投資バッファのマイナス
  - 出: 支出 / 貯金の先取り入金 / 軍資金の補充 / 投資の買付・バッファ拠出
  - 無関係: 遊技の勝ち負け(軍資金内で完結・isNetEntryで家計簿からも除外済み) / スイープ(軍資金→貯金)
  手で設定するのは**起点残高(S.wallet.seed)のみ**。
実装: `walletFlows(asOf)`/`walletBalance(asOf)`/`walletForecast()`。資産タブは総資産に所持金を含め(3分割表示)、所持金カードに**月末見込みと不足アラート**(v3.34の帯文法で「予定を払えません」+不足額・カード枠も赤に)。所持金画面は起点残高の設定と**全流入出の内訳**を表示。移動そのものは各ポットの画面で行う設計(二重の入口を作らない)。**軍資金に「所持金へ戻す」(type:"return")を新設**——従来はスイープ(→貯金)しか出口が無かった。検証9/9
- v3.80 **見出しを「当月収支」へ差し戻し(ユーザー指摘)**: v3.62で「当月残金=収支−先取り(max実績,計画)」としたが、**収支は収入−支出という定義の明確な事実であり、計画を引いて実態を模擬するのは事実と意図の混在**だった。手元にいくら残るかは所持金(次項の構想)が語るべき役割。先取りの控除と注記を撤去し、純粋な収支に戻した。貯金・投資の枠表示は従来通り(役割を分離)

- v3.79 **「変動消費を自動設定」ボタンを撤去(ユーザー指摘)**: v3.73で予算設定画面に変動消費の金額|割合を持たせた時点で、滝の自動設定は**同じ値を別経路で上書きする重複機能**になっていた(押すと設定画面の値が消える)。娯楽と同じく**設定値から毎月自動で解決**されるのが正しい。wfCalc/wfApply/wfapplyハンドラ/内訳画面の行を全撤去。**最終的なモデル(確定)**: 可処分=収入(予定込)−固定費 / 変動消費・娯楽=budgetAmt(金額|割合の小さい方) / 残余=可処分−変動消費−娯楽 → preSplit()が貯金・投資へ按分。**枠は毎月自動で解決され、押すボタンは無い**。S.waterfall.varMaxは参照が消えたが旧データに残っても無害。検証7/7(収入変動での自動追随・頭打ち分が貯金へ流れることを確認)
- v3.78 **月次固定費の日付欄を「初回の引き落とし日」(日付入力)へ(ユーザー設計・v3.77の上位互換)**: v3.77はラベルを直しただけで**入力の型が間違っているという本質**を見落としていた(ユーザー指摘)。日付で受ければ「毎月何日か」と「いつから始まるか」の両方が決まり、**翌月開始のサブスクに対応**でき、`0815`のような誤入力も構造的に消える。実装: フォームはtype="date"(fx-start)に統一し、fixedOccurrencesは**startがあれば日も位相もそこから取る**(v3.71の「周期>1のときだけstart」という特例を撤廃し全ケースで統一)。保存時もstartから日を導出し常に保持。編集時にstartが無い旧データは**当月×dayの日付を合成**して日付欄に出す(後方互換)。周期>1のときに別途出していた開始日欄は日付欄が兼ねるため撤去。注記は「2026/9 から 2ヶ月ごと 27 日に計上されます」と解決結果を明示。検証9/9(当月開始・翌月開始・隔月の位相・旧データ)
- v3.77 **固定費の「発生日」欄が誤解を招く(ユーザー発見・事故の手前で捕捉)**: 月次の日付欄は**何日か(1-31)**を入れる欄なのに、ラベルが「発生日」で終了日がカレンダー入力のため、日付だと誤解して`0815`のような値を入れやすい。保存時に31へ丸められ、**15日のつもりが月末計上になる**silentな事故。対処: ラベルを**「毎月何日(1-31)」**へ、placeholderを"1-31"→"15"へ、min/max属性を付与し、**入力直下に「毎月 ◯ 日に計上されます」を即時表示**(31超なら丸め警告も併記)。**教訓: 単位が自明でない数値欄は、解決後の値をその場に出す**——検証より予防が安い
- v3.75/3.76 **予算設定画面の新設+按分基準の修正(実機フィードバック4件)**: ①**家計簿カードから割合の併記を撤去**(見づらい)。設定値は設定画面で確認する ②**【バグ】貯金の按分額が小さすぎた**(¥4,535問題)——原因は変動消費が旧来の絶対値のまま残っていたことと、按分の基準設計。**目標は「枠を使い切った時に残る額」で固定**(可処分−娯楽枠−変動消費枠)。実績ベース(可処分−今の支出)にすると月初に巨大な数字が出て日々縮み、目標として機能しない。使い切らなければ実績が目標を上回り「達成」になる ③**予算設定画面(UI.screen="budget")を新設**——金額と割合を**別々の入力欄**に分離(複合文字列の手打ちは直感的でないとの指摘)。保存時に"金額|割合"へ合成し、budParts()/budJoin()で分解・合成。貯金/投資の按分比も同画面に集約し、**今月の解決額をその場でプレビュー**(「今月はこの枠 → ¥X」「今月の残り ¥X → 貯金¥Y / 投資¥Z」)。予算バーの「＋上限を設定」と貯金/投資行のタップから遷移 ④**貯金→投資の移行手段**: 按分比を貯金100:投資0 →(目標達成後)→ 0:100 と移すだけ。画面に注記済み ⑤按分の丸めで合計が残余を超えないようMath.min+差分方式に(6パターンで合計一致を確認)
- v3.74 **複合指定(小さい方)+貯金/投資を残余の按分へ(ユーザー設計・思想の完成形)**: ①**複合指定`"30000|30%"`は小さい方を採る**。通常収入では金額の枠が効き、低収入月だけ割合が自動で絞る——**割合は増収時の恩恵でなく下方リスクへの備え**というユーザーの意図を正確に実装(私は当初これを取り違え、「割合は増収で膨らむから危険」と警告していた)。単独指定は従来通り。②**貯金・投資は枠(上限/下限)を廃し「残余の按分比」へ**——可処分−娯楽−変動消費=残余を、sink:investの比で割る(比は合計100でなくても正規化)。上限も下限も不要というユーザー指摘が正しく、**生活水準を決める枠を固定すれば増収分の行き場は貯金と投資しかなくなる**——規律でなく構造で担保する仕組み。③滝(wfCalc)は按分と循環するためpre=0とし、上限側のみ解決する形に整理。**確定した設計(蒸し返さない)**: 娯楽・変動消費=複合の上限 / 貯金・投資=残余の按分比。検証: 通常月(可処分126,029→娯楽30,000・変動56,713・残余39,316)、低収入月(可処分21,029→娯楽6,309に自動縮小・赤字にならない)、増収月(娯楽と変動が頭打ち→増収分171,029が全て投資へ)。全シナリオ通過
- v3.73 **全ての予算枠を金額/割合の両対応へ(ユーザー設計)**: 「変動消費も娯楽も貯金も、収入−固定費を割合で割り振るべき」との提案。**基準=可処分(その月の収入(予定込) − 固定費)**を`disposableBase(mk)`で算出し、`budgetAmt(val, base)`が"20%"なら可処分×20%、"30000"なら金額として一律に解決する。対象は**変動消費・娯楽(S.budgets)/貯金・投資・変動消費上限(S.waterfall)**の全枠。表示は「¥25,206 (20%)」と実額+割合を併記。旧データ(素の金額)は無改修で動く後方互換。**設計上の要諦(蒸し返さない)**: 割合の向きはカテゴリで異なる——**娯楽・変動消費は上限**(増収が自動で娯楽に流れるのは漏れ係数が検出するための「漏れ」そのもの。良い月も頭打ちにしたければ金額固定にする) **貯金・投資は下限**(悪い月も死守し良い月は増える)。ユーザーの当面の設定意図は「娯楽MAX3万・変動消費MAX9万を金額で固定し、残余が貯金に流れる」形——この場合は娯楽と変動消費上限を金額、貯金を割合または金額にする。検証10/10
- v3.72 **【設計バグ修正】過去の月に「実行されなかった計画」を引いていた(ユーザー指摘)**: v3.62の`max(実績,目標)`は**進行中の月にだけ正しい規則**だった。終わった月に未実行の貯金計画50,000を引くため、7月の残金が実際の+19,366でなく**−30,634**と表示されていた=事実の歪曲。**過去の月は実績のみ、当月・未来は予定込み**に変更(アプリ全体の「過去は事実/未来は予定」原則に揃えた)。貯金・投資枠の残り表記も過去月は「あと」→**「未達」**(もう追加できないため)、見出し注記も「(実績)」を付記。**実装上の罠**: `monthKey()`は**表示中の月**を返すため`mk < monthKey()`は常に偽になる。実際の今月との比較には`todayStr().slice(0,7)`を使うこと。検証7/7
- v3.71 **隔月以上の固定費に開始日(位相の起点)を追加(ユーザー提案)**: v3.70で残った既知の欠陥——周期>1の位相が「編集した月」起点だったため、7/9/11月の請求を8月に設定すると8/10/12月にずれた。週次・隔週には既に起点日があるのに月次周期だけ無い不整合でもあった。**開始日の年月から stepM 刻みで進め、今月以降の最初の該当月から生成**(過去は生成しない)。開始日が無ければ従来通り今月起点、every=1では無視。フォームは**周期>1のときだけ開始日欄を表示**し「請求のある月を指定してください。日にちは発生日で決まります」と注記。保存時にstartを保持(従来は月次で捨てていた)。一覧のラベルも「2ヶ月ごと 27日(2026/09〜)」と起点を表示。検証4/4
- v3.70 **【バグ修正】固定費の周期(隔月)が保存されない**: 症状=水道を2ヶ月ごとにしたのに毎月計上される。原因は**`fx-every`のoninputバインド漏れ**——fx-day/fx-amountには有るのにこれだけ無く、周期に2を入力しても`f.every`は空のまま。**頻度ボタンや休日設定をタップした瞬間に再描画が走り、入力値が消えて毎月に戻っていた**(保存時はDOMから直読みするため、再描画を挟まずに保存すれば偶然通る=再現性が低く気付きにくい類)。fx-everyとfx-end(同じく漏れていた)にバインドを追加。fixedOccurrencesの計算自体は正しかった(every=2→8/10/12月)。**この位相問題はv3.71の開始日で解決済み**。**教訓: フォーム入力欄を追加/改修した時は必ずoninputバインドの有無を確認する**(v3.26のフォーム再構築時に見落とした)。検証4/4
- v3.69 **滝の基準を「前月の収入」→「当月の収入(予定込)」へ(噛み合わない問題の真因)**: ユーザー指摘「上の残金と下の予算が噛み合わない」の原因は**カレンダー月ではなく、2つの数字が違う収入を見ていたこと**(残金=当月収入300,000 / 変動消費枠=前月収入351,390)。前月ベースは月初に当月収入が0で計算できなかった名残で、**v3.59の定期収入登録により制約は消えていた**。当月ベースに揃えた結果、**「収入 = 固定費 + 変動消費枠 + 娯楽枠 + 先取り」という一つの式の別表示**になり、全枠を使い切ると残金が0に収束する(検証で確認)。当月の収入がまだ無い月のみ前月へフォールバック(basedOnPrevフラグで説明文も出し分け)。**給料日→給料日の会計期間への移行は見送り**: ①症状の原因は期間でなく基準のズレで、それは解消済み ②monthKey()基盤の全集計(leakAgg/固定費生成/明細/ふるさと納税の年/監査資料)に波及する大改修 ③給与明細の月度・税・ふるさと納税など外部の事実がカレンダー基準 ④当人の固定費は全て15日以降(23/27/28日)に落ちるためカレンダー月でも給与→固定費は同一月に収まる。数ヶ月運用して境界がまだ不自然なら再検討
- v3.68 **ジャンル内枠を割合指定にも対応(ユーザー提案)**: 滝の反転で**変動消費の枠が月ごとに動くようになった**ため(8月51,068 / 余裕月90,000)、固定額の内枠は月により意味が変わっていた(35,000は51,068の月なら69%、90,000の月なら39%)。**"35%"と入力すれば親の枠に自動追随**、"35000"なら従来通り固定額(通信費など固定が自然なものと併用可)。実装は`genreCapAmt(val, baseCap)`/`genreCapIsPct(val)`の2ヘルパで、**データモデルは文字列のまま**(末尾%の有無で判別)——既存の金額指定は無改修で動く後方互換。表示は「食費 ¥1,521 / ¥17,874 (35%)」と実額+割合を併記。不正入力は枠を作らない。検証15/15。**貯金の自動入金は不採用で確定**: 銀行で移していないのにアプリ上だけ貯金が増えると帳簿が現実から乖離するため(投資の時価不追随・ふるさと納税の限度額を計算しないのと同じ境界線)。手動運用+15日の儀式化で対応
- v3.67 **貯金/投資枠を予算カードから直接設定+名称の平易化(ユーザー指摘2件)**: ①**「滝で決める」→「変動消費を自動設定」**。「滝」は開発上の内輪の呼び名で画面上では意味が伝わらない。説明も「前月収入から固定費・貯金・投資・娯楽枠を引いた残りを上限にします」へ。プロンプト文言も「先取り額」→「貯金の月間枠」等に統一 ②**貯金・投資の行を常時表示**(v3.62は枠0で非表示にしたため、設定する場所自体が見えず「枠が出ない」状態を招いた=UXミス)。未設定時は変動消費・娯楽と同じ「＋ 上限を設定」文法で表示し、**その場のタップ(data-a=setpre)で直接設定**できる。設定済みならバー表示+タップで各画面へ、という既存の文法通り。**教訓: 「0なら隠す」は設定手段が他にある時のみ許される**。唯一の入口を隠すと機能ごと死ぬ。検証7/7
- v3.66 **滝の反転(設計の転換点・ユーザー設計)**: 旧=貯金/投資を先に決めて変動消費を逆算 → 収入が変動する月(所定内144〜176hで6万円差)に変動消費がマイナスになり統制が破綻していた。新=**先取り(貯金)を先に取り、残余が変動消費。ただし上限(既定9万)でクリップ**。金が無い月は変動消費が自動で絞られ、余る月は9万で頭打ち——**破れない規律**の条件を満たす(軍資金システムと同じ思想)。**確定した運用(月2回)**: ①給料日前日=口座残高の20万超を貯金へ(先月の使い残しの回収・Tallyは残高を持たないので台帳外) ②給料日当日=先取り(=wf.sink)。バッファ20万は月中の運転資金として機能。実装: **`wfCalc()`に計算を一元化**し、wfApplyと内訳画面の表示が同じ関数を呼ぶ(表示と適用の乖離を構造的に防ぐ・v3.60の教訓)。定数は sink=先取り / invest=投資 / **varMax=変動消費の上限(新設)** の3つ。残余がマイナスでも変動消費は0でクリップ。toastは残余なし/頭打ち/通常で文言を出し分け。検証13/13。**現実の数値(2026-08時点)**: 前月収入341,560 − 固定210,492 − 先取り50,000 − 娯楽30,000 = **残余51,068**。9万の上限には届かず、7月の変動消費実績105,368の半分。固定費のBASE FOOD 20,000は食費と二重構造のため削減の最有力候補
- v3.65 **【バグ修正】ジャンル内枠の「枠」ボタンが押せない**: v3.64のgcapハンドラが`ggroups`(viewCatDrillのローカル変数)を参照しており、bind側からは見えず**ReferenceErrorでハンドラが死亡**→親行のdrillstoreだけが発火し「押すと行が展開するだけ」になっていた。未分類額をハンドラ内で自前集計する形に修正。あわせて**bindCatDrillの委譲にイベント引数を渡しgcapでstopPropagation**(子ボタンと親行の同時発火を防止)。**教訓(重要): bind関数の中からview関数のローカル変数を参照しない**——view/bindが分離した構造では静かに死ぬ。ハンドラが必要とするデータはS(状態)から再計算するか、data-*属性で運ぶこと。検証5/5
- v3.64 **ジャンル内枠(食費上限)**: 7月の変動消費105,368の分析から——食費52,338(20件・1件平均2,617円)が半分を占め、固定費側にBASE FOOD 20,000があるのに外食が上乗せされる二重構造が判明(なお医療29,000を除けば76,368で枠内。「9万が高い」のでなく「食費が構造的に膨らむ」が正しい診断)。**変動消費9万は据え置き、その中に内枠を設ける**方式を採用(9万→7万への引き下げは医療のような不可避支出が来た月に即破綻し統制が形骸化するため)。実装: `S.genreCaps{ジャンル名:上限}`を新設し、**予算カードの変動消費バー直下に細いサブバー**(独立行にしないのは親の内訳だから)。集計はジャンル辞書(店→ジャンル・チェーン単位)で、固定費と未分類は混入しない。設定は内訳画面のジャンルモードの各行の「枠」ボタンから、**未分類の店がある時は金額を添えて警告**(割当漏れがあると枠が実態より小さく見えるため)。0で解除。検証7/7
- v3.63 **【バグ修正】内訳画面の「滝で決める」が押せない**: v3.60で行を内訳画面へ移設した際、**bindCatDrillにwfapplyハンドラを持って行かなかった**ため無反応だった(滝の定数を設定する唯一の入口が死んでいた=予算カードの貯金枠が永遠に出ない状態)。**wfapplyの実体を`wfApply()`関数に括り出し、メインと内訳の両ハンドラから呼ぶ**形に統一(2箇所に同じ実装を書けば必ずずれる——v3.60/v3.62と同じ教訓の3度目)。あわせて警告文を「定数(投資/積立)」→「投資・貯金の月額」へ。**教訓: UI要素を別画面へ移す時はハンドラの所属も必ず移す**(画面ごとにbind関数が分かれている構造では、行だけ動かすと静かに死ぬ)
- v3.62 **当月残金化+予算カードを滝の全体像へ(ユーザー設計)**: ①見出し「当月収支」→**「当月残金」**(先取り設定時のみ・未設定なら従来表記)。収支は純資産の増分だが先取り分は生活口座に残らず「いくら残るか」を答えられていなかった。**先取りは`max(実績,目標)`で控除**——実績だけだと月初過大→入金の瞬間に急落し、財務状況が不変なのに数字が暴れる。支出の予定込みと同じ扱いに揃えた(検証: 50,000入金しても残金不変、目標超過分のみ追加控除) ②**先取り1枠→貯金枠・投資枠の2枠に分割**。上限は滝の定数がそのまま入る(wf.sink=貯金/wf.invest=投資)。タップで各画面へ。実績データは新設せず`securedSplit(mk)`で分離(貯金=savings入金+bank sweep / 投資=holdings買付+bufferTxns)、**leakAgg.securedもこの関数の合算に統一**(同じ数字を2箇所で計算しない・v3.60の教訓を適用) ③滝の定数プロンプトを「投資の月額/貯金の月額」に統一し、それぞれ枠の上限になる旨を明記。各枠は上限も実績も0なら非表示。予算カードは変動消費・娯楽・貯金・投資の4本で滝の全体像を表す。実物検証9/9
- v3.61 **予算カードに3行目「先取り」(ユーザー提案)+滝の定数を実態へ読み替え**: 発端=「投資は一度も記録していない・当面は投資でなく貯金」。滝の定数`投資110,000`が実際には出ていかない金を引いており、かつ**先取り分が画面のどこにも出ないため「¥89,508残る」が実態と乖離**していた(先取り後は口座に残らない)。**データ源は新設せず`leakAgg().secured`を使用**——投資買付・バッファ・貯金入金・銀行スイープの当月合計を既に集計しており、記帳負担ゼロで実績が取れる。表示は予算カードの文法通り**実績/目標のバー**、ただし**消費娯楽=「超えるな」に対し先取り=「届かせろ」なので色はteal・残りは「あと」表記・達成でその旨**。タップで貯金画面へ。目標も実績も0なら非表示。**家計簿の支出には含めない**(貯金は消費でなく移転・二重計上を検証で確認)。滝の定数プロンプトも「投資の月額」→「毎月の先取り額(貯金・投資へ回す分)」に改名、内訳画面の説明も「固定費・先取り・娯楽枠」へ。**未解決の論点**: 貯金への先取りを月1回記帳(貯金画面の先取り入金)する運用が定着するかは要観察。定着しなければこの行は常に¥0となり計器として死ぬ。実物検証5/5
- v3.60 **滝の自動案を主画面→内訳画面へ移設(ユーザー指摘「表記がダサい・ここに書かなくても良い」)**: 主画面の予算カードは「今いくら使ったか」を見る場所で、設定の提案が常駐するのは場違いだった。**内訳画面(消費のみ・予算変更ボタンと同じ画面=予算管理の文脈)へ移設**し、式の羅列「自動案: 前月給与−固定−投資−娯楽−積立」を廃して**「滝で決める ¥X ›」+説明文1行**の行動として読める形へ。適用中は矢印を消して沈む表示。**移設時に実バグを検出・修正**: 当初`list.filter(e=>e.fixedId!=null)`で固定費を数えたが、listは変動消費のみで固定費を含まないため常に0となり表示額¥166,560/適用額¥118,560と食い違っていた→**wfapplyハンドラと完全に同一の式**(当月エントリから直接集計)に統一。**教訓: 同じ数字を2箇所で計算する時は式を文字通り同一にする**(表示と実行の乖離は最悪の不具合)。実物レンダリング検証3/3
- v3.59 **定期収入の発見可能性を改善(新機能ではない)**: 「月初に支出予定だけ入って収支が大きくマイナス=いくら残るか判断できない」との指摘。調査の結果**機能は既に存在**(固定費フォームに収入/支出トグル・genFixedEntriesは`type: tpl.type`をそのまま使うため定期収入は正しく予定込みに乗る)。発見できない原因は画面説明が支出しか語っていないことだったため、固定費introに「給与など定期収入も登録できます(収入に切替)」の1行を追加。**副作用検証済み**: 漏れ係数は増収30,000円以上のペアのみ対象なので毎月同額の定期収入では発火しない/滝の自動案のprevSalは前月のみ参照/監査資料の平均支出は当月除外。**運用上のコスト**: 給料日通過後は実額に直す一手間が必要(光熱費と同じパターン・明細に出るので気付ける)
- v3.58 **【重大バグ修正】月替わりでアプリが操作不能になる**: 症状=8/1にアプリが記帳画面で固まり他タブへ移動できない。原因=**v3.48で固定費行を予算カードから撤去した際、`const fxSpent`の定義ごと消えたのに滝の自動案が参照し続けていた**(ReferenceError)。`if(prevSal>0)`(前月給与>0)の内側だったため7月は沈黙し、**8月になり前月=7月に給与が入った瞬間に発火**——ユーザーの「8月になった影響か」は正確な直感だった。viewMainが例外→DOM更新されず記帳画面のまま=「遷移できない」に見えていた。fxSpentの定義を該当ブロック内に再掲して修正 ②**描画例外の安全網を新設**: _renderBodyを_renderScreenに分離しtry/catchで包む。例外時はエラー画面(原因メッセージ付き・隠さない)へ退避し**ナビバーを強制表示**して健全なタブへ逃がす。タブハンドラに`UI.screen="main"`を追加(通常は無害・エラー画面からの脱出路)。**教訓: 変数定義を含むブロックを削除する時は、その変数の全参照を確認する**(grepで`fxSpent`を引けば即座に分かった)。**検証手法の確立**: DOMスタブ+vm.runInThisContextで実物のview関数群を直接実行できる(5シナリオ×18ビュー+記帳7バリエーション+障害注入5項目=全通過)。月替わり・年跨ぎ・データ皆無の回帰確認は今後もこの方式で
- v3.57 **カスケードを「欄→直下チップ」の対構造へ(ユーザーの比較指摘が正)**: 交通コンポーザは入力欄の直下に候補チップが付いて対になっているのに、通常モードはチップが上・対応する入力欄が下に離れていた=汚さの正体。**店名欄→直下によく使う店チップ、品目欄→直下に品目チップ、その下に金額チップ**へ再配置し交通と同じ文法に統一。チップ有無でmargin可変(6/8/14px)。ラベルは欄のplaceholderが担うため店・品目のチップ見出しを省略(金額のみラベル維持=対応欄が画面上部で離れているため)。**副産物: 交通モード中の非表示条件が不要に**——チップはfieldsHtml内に入り、交通時はcomposerHtmlに差し替わるので構造的に消える(v3.56の条件分岐を削除、chipsHtml変数も廃止)
- v3.56 v3.55の実機フィードバック3件: ①**バグ: 交通モード中もカスケードが描画され店の選択が残っていた**——保存時に店名/品目は合成で上書きされるため選択自体が無意味。`f.tmode`が立つ間はchipsHtmlを描かず、tmodeハンドラで**f.store/f.itemをクリア** ②**店チップを既定5件+展開トグル**(UI.chipMore・「＋他N件」/「▾ 閉じる」・取得上限は12へ)。8件常時は4行占めて見通しを失っていた。品目も6件に抑制 ③**交通行をカスケードより上へ移動**——交通はモード切替(下の内容を変えるもの)なので階層上位が正しい。これで浮きが解消し「分類→モード→絞り込み→入力欄」の順に整った。検証6/6
- v3.55 **「よく使う」を3段カスケードへ再構成(ユーザー設計)**: 旧の合体チップ(店名+品目+¥を1本に連結)は文字の壁で使用困難+「店から絞る」という自然な思考順に逆らっていた。新=**段階的開示**: ①よく使う店(頻度順8) → ②選んだ店の品目(チェーン横断・chainKeyOf) → ③その店+品目の履歴金額(上位4)。店を選ぶまで品目は出ず、品目を選ぶまで金額は出ない。選択済みはsel表示。**v3.30の資産を全て移植**: 経路品目「A→B」の逆方向自動生成、逆方向は往路の運賃を流用(履歴が無いため)、店/店+品目からの分類自動推定(旧チップのcat設定を継承・段階ごとに精度が上がる)。**entryUsualChipsは完全削除**(死コードを残さない)。新設: CHIP_SRC_OK/chipItemOf/entryChipStores/entryChipItems/entryChipAmounts/entryChipCat。ハンドラはustore/uitem/uamtの3本。実物抽出テスト9/9
- v3.54 **記録画面から代打への動線+幽霊チップのバグ修正**: ①**バグ**: entryUsualChipsがslot/mahjong/fixedを除外しながら**daidaを除外し忘れ**、収入フォームの「よく使う」に「代打 @店名 ¥X」チップが出現していた——タップすると**セッションを持たない幽霊代打エントリ**(src無し=遊技カード非計上・台記録なし・セッション未リンク)が生まれる罠。daidaを除外条件に追加 ②**動線はエントリ作成でなくrouting**: 収入フォーム(新規時のみ)に「代打の記録はセッションで入力 →」ボタン(data-a=todai)を置き、mode="dai"の新規セッションへ飛ばす(フォームの日付を引き継ぎ)。**代打の真実の所在はセッション、家計簿の行は派生物**という原則をUIで表現——フォーム側で代打エントリを作れるようにすると二重の真実源になり①の幽霊と同じ病になる。検証3/3
- v3.53 **家計簿→代打セッションの動線(潜在バグ修正を兼ねる)**: eeditはsrc=slot/mahjongをセッション編集へ飛ばす分岐を持つが**daidaだけ漏れていた**——汎用記帳フォームが開き、そこでの金額編集が次のセッション保存(linkId再生成)で静かに上書きされる潜在バグ。daidaもslotと同じlinkId("slot-"+id)なので同分岐に合流させて修正。これによりユーザー提案の「家計簿の収入から代打へ飛ぶ動線」が成立。**ただし遊技タブからの代打除去は不採用で確定**: ①代打セッションは打った台・客の玉・交換設定・実働時間という遊技運用データを持ち、家計簿明細(副業+¥X @店名の1行)では到達も閲覧もできない ②保存直後の記帳確認先が消える(ユーザー自身が指摘した論点) ③遊技の一覧が「稼働の完全な記録」でなくなり、一部のセッションだけ載る方が現状より悪い不整合になる。そもそも遊技タブは稼働ログでカードだけがP&L——v3.52でカードを整合させたことで概念のズレは解消済み
- v3.52 **遊技カードに代打報酬の従属行(違和感の真因対処)**: 「代打が一覧に出るのに遊技収支に乗らない」違和感の原因は一覧でなく**サマリーカードの沈黙**(代打はsrc=daida/category=副業で家計簿へ流れる実在の金なのにカードが語らない→金が消えて見える)。一覧は入力導線・記帳確認として不可欠なので触らず、カード下部に**「代打報酬 +¥X → 家計簿の副業へ」**を従属行として追加(金額を見せ・行き先を明記し・遊技収支の外であることを位置で示す)。代打のみの月も表示(margin条件分岐)、代打なしの月は非表示。②抽選タグに枠線指定を追加(v3.51で.tagの枠線を忘れ素テキストが浮いていた——定期タグと同文法へ)
- v3.51 **抽選のみの代打+セッション店名の予測(実務起点の要求)**: ①**抽選はモードでなく代打の下位フラグ**(s.lotteryOnly)——第4モード化しないのは集計上代打の一種として数えたい+既存データの後方互換のため。代打選択時のみトグル表示、ONで**交換設定と台データを畳み**報酬/店名/経費だけの姿に。セッションカードに「抽選」タグ ②**店名予測チップ**(sessStoreHints: 登録店舗S.storesを最優先+過去セッション店名を頻度順・上位5)——パートナー名チップと同じ文法。表記ゆれを構造で抑える狙い。検証6/6
- v3.50 **予定金額の内訳移管+収支カードの遊技寸法化(ユーザー発案)**: 収支カードから予定行を全撤去し、収入/支出ハーフを遊技カードと同寸(ラベル11px+mb3・数字18px太字)へ——両タブの顔が揃った。予定は内訳パネルへ: セクション見出し右に**予定計**(>0時)、各行に**「うち予定 ¥X」subline**(>0時・bdRowにfut引数追加)。固定行は既存の支払済/この後の予定sublineが担うため二重表示回避(fut=0渡し)
- v3.49 v3.48実機フィードバック2件: ①**内訳金額の分類色を撤回**——7個の色数字の積み上げは実機でうるさい(ユーザーの事前警告「色変えまくると煩い」が正・予算バー前例は視野に1個で密度が別物)。**色はバーに一元化、金額は通常色**が確定規約 ②収支カードの**「予定 ¥0」行を根絶**——hasFuture全体フラグでの機械表示をやめ、futIncome/futExpense>0のサイド別条件に。無意味な行の対称性より意味の密度
- v3.48 **内訳UIの作り直し(v3.47への3指摘対応)**: ①ハーフタップ+シェブロン廃止→収支カード下端の「内訳を見る ▸/内訳を隠す ▾」1本 ②排他アコーディオン廃止→**1タップで支出内訳+収入内訳を一体1枚展開**(UI.bdOpenはboolean化) ③金額は分類色(固定=灰・変動=赤・娯楽=琥珀・承認=青・収入=teal——予算バーの数字と同じ既存文法・新色なし) ④**固定費行を予算カードから撤去**(固定費は予算ではない・ユーザー指摘)→内訳の固定行がタップで固定費画面へ(支払済/この後の予定のsublineも移設)。常設導線はツール家計簿バンドの固定費カードが担保。予算カードは変動消費+娯楽の純粋な予算バー2本に。先頭行の上罫線を既存インデックスiの条件で抑止
- v3.47 **収支内訳のアコーディオン化(ユーザーの対称性指摘が正)**: 常設の収入内訳バンドを廃止し、収支カードの**収入/支出それぞれをタップで展開**(▸▾・data-a=bdtoggle・UI.bdOpenアコーディオン=片方開けば他方閉じ・セッション内保持)。**支出側は固定/変動消費/娯楽/承認の4区分**——承認合計の唯一の可視化点(バー対象外でどこにも出ていなかった。ふるさと納税の受け皿)。%分母を月内合計に統一(旧: 実績分母×予定込分子で100%超え得た潜在バグを修正)。検証4/4
- v3.46 **明細の日別折り畳み(ユーザー発案)**: 日ヘッダーがカード行化し ▸日付・件数・**日別純額**(収入−支出・符号色付き)を表示、タップで開閉(data-a=dtoggle・UI.dayOpenセッション内保持)。**既定=今日のみ展開**(記帳直後の確認一瞥を守る)、過去と予定日は畳み。予定日はopacity減。render()のscroll保存復元(741-744行)が効くため開閉でジャンプしない。明細空状態の「右下の＋」旧FAB化石も一掃(v3.39の掃討漏れ)
- v3.45 **アイコン戦争終戦**: 通帳は実機でラップトップ誤読(予見リスクが的中)。結論=**通帳は22pxで表現不能**(遠近=ラップトップ/正面=縦長矩形で電卓と族衝突、の二択詰み)。承認済みの**開き本へ復帰**し確定。最終布陣: 開き本/ダイス/鉛筆/棒グラフ/電卓——**この5点は確定、以後蒸し返さない**
- v3.44 家計簿アイコン=**開いた通帳**(ユーザー案採用): 資産が棒グラフに確定し本族の席が空いたため、v3.41で退けた通帳案が家計簿側で成立。上開き(横折り目)が通帳の識別子——上ページ台形+下ページ平行四辺形+記帳行3本。意味論も開き本より正確(タブの中身=記帳履歴)。残リスク=ラップトップ誤読、実機判定待ち
- v3.43 資産アイコン確定: ユーザー発見のiSPEED参照で**棒グラフ**(縦棒5本・押し引きしつつ右肩上がり・太線2.4丸端)。純化後の資産(増減しながら積み上がる金)の意味論に一致、アプリロゴ(タリーマーク)と縦棒族で調和。コイン案は棄却
- v3.42 **資産タブの純化(ユーザーの根本指摘が正)**: アイコン3連敗(シリンダー/金庫/紙幣)の真因=**タブが概念の寄せ集めで一枚の絵にならない**こと。処方は改名でなく中身の整理——**ふるさと納税残枠と漏れ係数カードをツールの家計簿バンドへ移設**(ほしい物の直後・ハンドラはメイン委譲のため無改修)。資産タブ=軍資金・貯金・投資の純粋な「金の置き場」となり、名前が正しくなった。アイコンは**コイン(円+¥二本棒)**で確定挑戦。教訓: **アイコンが描けない時は絵でなく情報設計を疑う**
- v3.41 資産アイコン再挑戦: 金庫は22pxで金庫と読めず(ユーザー判定)。通帳案は本族のため家計簿(開き本)か電卓(縦長)と必ず族被りする旨を説明し不採用。**紙幣(横長札+¥一本棒+両端点)**へ。家計簿の開き本は承認済み。次弾候補=コイン(円+¥)
- v3.40 **タブアイコン刷新(家計簿・資産)**: 診断=①資産はDBシリンダーで金の意味論ゼロ ②家計簿レシートは電卓(ツール)と縦長矩形+内部線でシルエット衝突。新=**家計簿:開いた帳面(通帳・横広)/資産:金庫(正方形+ダイヤル+足)**。5タブの輪郭が全異形(横広本/ダイス/斜め鉛筆/正方金庫/縦長電卓)になり瞬間識別が成立。実機確認で違和感あれば微調整
- v3.39 ①**中央ボタンの統一(ユーザーの違和感が正)**: 旧=タブ文脈で行き先が変わる(遊技タブ→セッション/卓の直行、他→記帳)。新=**常に家計簿記帳(1ボタン=1動詞)**、名称「追加」→**「記録」**・アイコン⊕→鉛筆(TAB_ICONS.add)。遊技の新規作成は**タブ内破線カード**へ移設(スロット「＋新しいセッション」/麻雀「＋新しい卓」・data-a=slotnew/mjnew・UI.tab/gameSubも設定)。タップ数の退行ゼロ、遊技タブ滞在中の記帳は2→1タップに改善。空状態の「右下の＋から追加」という旧FAB残滓文言も一掃 ②**折返し第3波**: v3.37走査の50字閾値が甘く34〜49字帯に7件残存(軍資金警告/経費取込注意/麻雀空状態/投資メモ未記入/スコア表説明/ボーダー移動告知/自動退避説明)→全件行構造化。**走査閾値は34字が正**
- v3.38 **ふるさと納税・残枠カード(相談の結論=真っ二つに分割)**: **限度額の計算機能は不採用で確定**——①必要データ(額面年収・控除)がTallyに不在(記録は手取り) ②税制の毎年改正への追随義務を負えない(投資の時価不追随と同型の境界線) ③誤差=限度超過分の自腹という実金リスク。計算は外部の詳細シミュレーター年1回、**アプリは限度額1個(S.furusato={year,limit})の入力と当年寄付の全導出トラッキングに徹する**。記帳規約: **店名「ふるさと納税」固定**(サイト名は品目へ)・**分類=承認**(バー対象外=変動消費予算を爆殺しない)。資産タブの投資カード直下に残枠カード: 寄付済/限度額(tealバー)・超過時はv3.34帯文法「限度額オーバー(自己負担)」・年替わりで自動失効+再計算促し・タップ=prompt設定(0で解除)。導出テスト4/4(前年・他店名・収入の除外を確認)
- v3.37 **折返し整備・第2波(正規表現走査で漏れ8箇所検出→残0確認)**: ほしい物intro(目的文を先頭へ)/漏れ係数intro・読み方/軍資金intro/貯金intro/交換設定説明/機種データ空状態(空状態の定石として中央揃えのまま行構造化)/同期説明。**introの左揃えは維持で確定**——アプリ文法は「中央=画面タイトルのみ」で、列挙型ヘルプは中央だと破綻する。左寄りの違和感は全幅ジャスティファイ見た目からの新奇性由来と判断、残る場合も中央揃えでなくキャプション囲いで再検討する(蒸し返し注意)
- v3.36 **ヘルプ文の和文折返し整備(見た目調整フェーズ入り)**: iOS Safariには語境界折返し(Chromeのword-break:auto-phrase相当)が無く「試/算」等の語中折りが発生していた。**規約確定: ヘルプ文は1文≤約24字に圧縮し<br>で行構造化**(構造で語中折りを根絶・以後の新規ヘルプ文もこの規約で書く)。適用10箇所=ツール店舗データカード説明(1行化・詳細は遷移先introへ委譲)/固定費intro/ボーダー試算intro/削り率/時給の設定/区間説明/店舗データintro/店舗レート説明/エクスポート・インポート説明。+`.muted{text-wrap:pretty}`(iOS26+で有効・非対応環境は無視の安全な保険)
- v3.35 **超過時の色相乗っ取り廃止(ユーザー指摘)**: 従来は超過で娯楽バーまで赤化し分類の見分けが消えていた。三色システムの原則に従い**超過でも分類固有色を維持**(娯楽=琥珀帯・琥珀数字、消費=赤で実質不変)——警報は16px帯+「予算オーバー」+「超過 ¥X」の形状とラベルが担い、色相は分類の仕事。メイン2バー+内訳画面の合計に適用。帯文字色var(--bg)は琥珀地でもコントラスト良
- v3.34 **予算オーバー帯(MoneyForward参考・ユーザー発案)**: 予算バーが超過時のみ7px→16pxの赤帯に変化し「予算オーバー」を中央表示(文字色var(--bg)=両テーマでコントラスト確保)。非超過時は従来バー、下段「超過 ¥X」は温存(帯=状態・数字=量)。消費・娯楽共通、内訳画面の合計バーにも適用(同一文字列2箇所置換)。MFの結果予想(月末着地ペース予測)は今回移植せず(要望があれば)。このpushが改名後Deploy Tallyショートカットの実弾テストを兼ねる
- v3.33 **kakeibo残滓の掃討(リポジトリ改名Tally準備)**: CSV書き出し名→tally.csv、バックアップ名→tally_backup_。**KEY="kakeibo:v1"は意図的に不変更+ガードコメント付与**(内部ID・変更=全データ孤立の地雷・URL無関係)。sw.jsをv1.50へ(キャッシュ名tally-1.50のみ変更・ロジック不変・旧キャッシュはactivateで自動削除)。manifest/sw.jsレビュー完了=**絶対パスなしでrename安全を確認**(start_url/scope/ASSETS全て相対)。README.mdをTally版に刷新。移行手順: 未push分をpush→旧PWAでエクスポート→4ファイル(index/sw/README/HANDOFF)commit&push→GitHub Settingsで改名→Working Copyのremote URL更新+ローカル名Rename→Deploy Tallyショートカットのリポジトリ名修正→sai2943.github.io/Tally/でPWA再追加(アイコンはここで焼かれる)→インポート→旧PWA削除
- v3.32 **収入カテゴリ「家庭内」新設**(妻からの生活費入金の分類問題): 世帯内移転は既存4分類のどれでもないため新設。**配線の確定事項: leakAgg.salary=カテゴリ不問の全収入**→家庭内も翌月の滝自動案原資・漏れ係数に乗るが、支出側が既に家計混在(妻のカネで賄う費目を自分の口座から支払い)のため**収入側も入れて初めて家計一体で対称・正しい**——記帳しない選択の方が歪む。毎月定額なら固定費フォームの収入型で定期計上可
- v3.31 **交通コンポーザ(ユーザー発案)**: 記帳フォーム(新規支出のみ)に「交通で記帳」モードチップ[地下鉄|バス|電車|駐輪]。選択で店名/品目欄がコンポーザに置換——鉄道系=[乗車駅・停][降車駅・停]、駐輪=[場所]、各欄下に過去品目を→分解した**駅・停トークンの頻度順タップチップ**(transTokens・上位8・datalist不使用)。保存時に 店名=モード名(駐輪は場所)/品目=乗車→降車(駐輪は「駐輪」)/分類=消費 へ合成し従来の文字列規約に落とす——**分類は増やさない・データモデル無変更が要諦**(全集計配線が無改修で生存)。分類行はモード選択中非表示(消費固定)、よく使うチップのタップでモード自動解除、編集画面は素の店名/品目の一方通行。**表記規約を規律でなく構造で強制する関所**。店名は「地下鉄」で一本化(東西線/南北線区別の上書き確定——路線情報を使う下流なし)。実物関数テスト6/6
- v3.30 **「よく使う」チップ強化(乗降駅の入力コスト問題・車内からの要望)**: ①ペアの金額最頻値が**8割以上**を占めるならチップに¥同梱、タップで金額まで自動充填しフォーカス省略=完全1タップ(定額運賃・定期購入)。ばらつく品目は従来通り空欄+金額フォーカス(誤額の静かな混入防止)。8割閾値はタイプミス1件への耐性 ②**経路品目「A→B」の逆方向チップ自動生成**(同店名・同分類・運賃同額前提。履歴に逆方向が既在なら非生成=実績側が自力でランクイン。上限8) ③**運用規約: 乗降は品目「乗車駅→降車駅」で記録**(「→」が逆生成トリガー)。往路を1回手入力すれば復路は即チップ。店名は毎回同一表記に固定すること(重複キーはstore+item——表記ゆれはチップを分裂させる)。外部駅名DBは不採用(完全オフライン憲法・自分の履歴が辞書)。実物関数抽出テスト6/6。**入力パイプライン確定(アプリ外)**: 既知駅=予測変換で手打ち/未知駅・停=ユーザー既存の汎用スポット検索ショートカット(カテゴリリスト選択→現在地周辺検索→選択→名前をクリップボード)のリストに「駅」「バス停」を追加して名前をコピー→品目へペースト。未知はほぼ降車側で既知半分は手打ちするため、専用の2フェーズ状態機械(ride_state.txt方式)は過剰設計と判明し破棄——蒸し返さない。PWAへの直接注入はiOS仕様上不可(URL起動はSafariに逃げる)——クリップボードが到達可能な天井。未知バス停はAppleスポット検索に弱い→ポールの名前を手打ちが正。コピー名の「駅」接尾辞は経路ごとに自己一貫するのでチップ実害なし(気になる場合のみ既存ショートカットに`駅$`除去を挟む)
- v3.29 ①**ツールタブの投資バンド廃止**: v3.18以前の化石プレースホルダー(「準備中」)——約束は資産タブの投資台帳が果たし済みだった。受け皿コメント(旧1227行)も実態に更新 ②**genInvestAuditに「生活防衛資金・現金ポジション」セクション**: 貯金残高/直近3ヶ月平均の月間支出(遊技収支除く・**当月は意図的に除外**=進行月混入は残高○ヶ月分を過大評価し防衛計器が危険側に誤る・支出0月スキップ)→残高○ヶ月分/目標梯子(✓達成・▶現在+進捗%・○待機)/投資スタート残額(**画面ロードマップと同一計算式**)+監査観点6「防衛資金の十分性」。全導出・新規入力ゼロ。検証は実物関数をファイルから抽出して8/8。**設計判断確定: 貯金UIは投資画面へ移設しない**(貯金は引越し資金・冷蔵庫問題の積立を兼ねる多目的口座。画面統合はv3.24梯子で済、「貯金込みでAIに判断」は監査資料側で解決)——蒸し返さない
- v3.28 **navbarフェード帯**: v3.27の副作用(旧インジケーター行が実は約17pxスペーサー兼務→撤去でコンテンツ切断線とタブピルが密着)への回答。`#navbar::before`で上へ24px張り出す透明→bgグラデ(pointer-events:none)+padding-top6px。最下部スクロール時にフェードが覆うのは#app自身の下パディングなので最終カードは読める。詳細ノブは§4
- v3.27 保存インジケーターをタブ直上→**タブ直下**へ(ユーザー要望・703行付近の連結順反転`tabBar()+行`、padding上下も1/4→4/1反転)。--navh実測注入で追随全自動。※「ナビバーをさらに下げたい」は62pt問題そのもの=不可と再確認(--navpadは既に下限2pxに張り付き。隙間が見える事実自体が「iOS未修正」の証拠)
- v3.26 **固定費フォームの期日・金額グリッド再構築**(スクショ起点のダサい指摘): 旧9pxラベル+.sm小箱の3列詰め込み+孤立終了日(v2.95で麻雀画面から掃討した旧様式の最後の残党)を廃し、記帳フォームと同じ標準.inputの2列文法へ。月ごと=[発生日|周期]/[金額|終了日]の2×2、週次・曜日指定=[起点日|終了日]/[金額全幅]。`.fxg`/`.fxl`クラス新設、ラベル9px→11px(節ラベルと同格)、**全欄height:42px明示(v2.91原則)**——v3.25のmin-height:38px対症とインラインappearanceは撤去(高さ明示+全体則が恒久策)。ID無変更でバインド・保存ロジック無傷。4頻度全分岐でdiv開閉/ID配置を検証済み
- v3.25 **最終更新2件**: ①**Nヶ月周期**——freqボタンを「月ごと/毎週/2週/曜日指定」の4択に統合し、月ごと選択時は発生日の隣に「周期(ヶ月ごと)」数値入力(1-12クランプ・every>1のみ保存)。エンジンはstepM=(bimonthly互換2 : every)で旧2ヶ月テンプレ(水道)は無変換で動作、編集時にmonthly+every=2へ自動変換。ラベル「Nヶ月ごとX日」。検証3/3(3ヶ月・6ヶ月・bimonthly互換) ②**終了日の潰れ修正(再)**——v3.19の全幅化は誤診で、真因は空のdate入力をiOSが萎ませる挙動(発生日はplaceholderが箱を支えていた)。appearance:none+min-height:38pxで箱を固定し、幅は半分幅(開始日と同格)へ復帰。Amazon定期便の固定費化は接骨院と対照的に正解(発生パターンをAmazonが決定=機械予測可能)
- **【運用事故と教訓】Deploy Tallyの文字化け事件**: v3.24配布時にPages上のindex.htmlが文字化け→同じショートカットで再pushしたら回復。原因=共有シートが「ファイル実体/テキスト表現」のどちらで渡すかが非決定で、**テキスト経由の回だけエンコーディング変換で化ける**。そのテキスト受け取りは「Refusing write」誤診断時に保険で追加したもの(真犯人はOverwrite設定だったので不要な保険だった)。**恒久対策: 受け取り種類を「ファイル」のみに戻す**。共有シートにDeploy Tallyが出ない時=テキスト型の合図→ファイル保存してから共有。※化けてもlocalStorage/クラウドのデータは無傷(配布物と金庫は別)・アイコン削除だけは厳禁(倉庫ごと消える)
- v3.24 **先取りロードマップ**: 投資画面最上部に貯金目標の梯子(✓完了teal/▶現在amber+進捗バー/○待機)→最終段「投資スタート あと¥X」(未完了目標の合計−貯金残高、目標未設定はその旨表示)。全導出・新規状態ゼロ。**フェーズ切替のレバーは滝の定数再設定**(現状: 投資0/積立11万→引越し完了時に反転)。設計意図: ギャンブル欲(金を遊ばせるストレス)への対抗は我慢ではなく視線の付け替え——株価でなくカウントダウンを見る場所を作る。順序の正当性=防衛資金なき投資は強制売却リスクの購入
- v3.23 **滝方式の二段目(変動予算の自動導出)**: 変動予算=前月給与(leakAggから)−当月固定費(fxSpent)−投資月額−娯楽枠−目的積立。S.waterfall={invest,sink}(applyDataホワイトリスト済)。変動消費バー下に「自動案 ¥X ←適用/適用中」の行(カテゴリdivの**外側**=catdrillとのタップ衝突回避)。初回タップで定数2つをprompt、適用中に再タップで定数再設定。検証2/2(7月給与→52,060/8月40万→129,500)。**冷蔵庫問題の運用ルール確定: 積立型の承認買いは貯金deposit(毎月)+withdraw+メモ(購入時)で完結し家計簿に支出計上しない**(即金型=承認カテゴリ計上と二分)。**投資は定額で確定**(割合案はユーザー自身の機械実行ルールと矛盾のため棄却)
- v3.22 **漏れ係数(補償消費の計器・ユーザーの15年来の仮説検証装置)**: 完全導出レイヤー(新規入力ゼロ)。leakAgg(mk)={salary(net除外income), spend(net除外expense), gnet(遊技純益), secured(投資buys+Buffer正+貯金deposit+スイープ)}。ヘッドライン=直近の増収月ペア(前月比+3万以上)のΔ支出/Δ収入を%表示(60%↑赤/30%↑琥珀/未満teal)・残存%併記。月次テーブル12ヶ月(給与/支出/遊技/確保)。遊技セクション=勝ち月純益合計vs貯金スイープの回収率+外部補充(生活→遊技の逆流)を赤表示——軍資金の壁の検査器。資産タブにカード(直近係数 or 測定待ち)。検証5/5(Δ89,940/40,000→44%・確保151,000・回収率40%)。**8月給与着弾後が初測定**——雀荘時代の体感「漏れほぼ100%」との比較が本題
- v3.21 固定費行の説明文言(「意志力の外・一覧へ」)を削除——ラベル+金額+›のみに(UIのゴチャつき指摘対応)
- v3.20 **予算の3層化(固定費/変動消費/娯楽)=滝方式の一段目**: 予算カード先頭に固定費行(バー無し・支払済+予定の内訳・タップで固定費一覧data-a="fixed"再利用)。消費バーは**変動消費**に再定義(fixedId==nullのみ集計・ラベル変更)。判定基準=fixedIdの有無(入力変更ゼロ)。ドリルダウンはUI.drillVarOnly(消費入口でtrue)で固定除外・タイトル「変動消費の内訳」・**ジャンル分析も自動的に変動のみ**(固定は固定費一覧が担当)。**S.budgets["消費"]の意味が「変動の上限」に変化——ユーザーが数字を実態調整する必要あり**。tally-widget.js v2も同意味論で更新済み(bud()にe.fixedId==null・ラベル「変動消費」——Scriptable内の貼り替えが必要)。娯楽のfixedは現状無し(発生したら同様の扱い要検討)。滝の二段目(前月給与から変動予算を自動導出)は未着手
- v3.19 固定費まわりの実使用フィードバック3件: ①**固定費フォームの品目候補もライブ差し替え**(fx-store入力→fx-itemlist再構築。v3.17で「使用頻度低」とスコープ外にした手抜きがMax20登録で即バレ) ②終了日入力を全幅に(隣の空スペーサー撤去+width:100%) ③**明細編集→「この内容で固定費に登録」ボタン**(item/store/category/金額/日付の日を引き継ぎfreq=monthlyでfixForm初期化→固定費画面へ。「2回目の支払いが来た時に昇格させる」動線)。**設計判断の明文化: updateFixedの掃除は未来のみ**——過去日付の残骸(誤登録の17日分など)は実績と機械判別不能のため手動削除が正(過去保護の原則優先)
- v3.18 **投資台帳(時価を追わない設計・新年送り予定を実需で前倒し)**: S.invest={policy, holdings[{name,frame(つみたて/成長/特定/優待),qty,cost,unit,thesis,exit,note,buys[]}], bufferTxns[]}(applyDataホワイトリスト追加済み)。資産タブのプレースホルダーを投資カードに置換→viewInvest(方針textarea/NISA枠メーター=当年buysから導出/Buffer残高+積立・出動prompt/保有一覧)→viewInvestDetail(枠・単位切替/買付記録=qty・cost加算/シナリオ帳=理由・売却条件・メモ/買付履歴/削除)。**genInvestAudit()=AI監査用Markdown一括生成→クリップボード**(方針/保有+シナリオ/当年買付/NISA消化/Buffer履歴/監査観点5点の依頼文まで焼き込み——Tally自身はネット非接続の憲法維持、貼り先はClaude/Cowork)。設計思想: **時価は証券アプリの仕事・毎日の評価額表示は機械的積立の敵**(ボーダー理論)。検証3/3(NISA枠別当年集計・Buffer残高)
- v3.17 **品目予測の店文脈化**: entryItemHintsFor(store)——店名あり=そのチェーンの過去品目のみ頻度順(chainKeyOfをグローバル化・viewCatDrill内のchainKeyは局所重複のまま)、未知の店=空(的外れ全体人気を出さない)、店名なし=従来全体。記帳フォームは描画時反映+**e-store入力と同時にe-itemlistをライブ差し替え**、固定費フォームは描画時反映のみ(使用頻度低のため)。検証4/4(支店合流・頻度順・未知空・混入なし)
- v3.16 **ジャンル辞書(店→ジャンルのレンズ)**: S.genres={チェーン名:ジャンル名}(applyDataホワイトリスト追加済み=v3.13教訓の先回り適用)。内訳トグル3択化(チェーン集約/店舗別/ジャンル)。ジャンルモード=ジャンル行(降順)→展開でチェーン小計→チェーン行タップで割当チップ(スターター8種+既存+新規prompt+未分類へ)。**キーはチェーン名単位=マクドナルド1回の割当で全支店・全期間に遡及**。店名未入力もバケットとして割当可。UI.drillAssign(選択中チェーン)は入口/モード切替でリセット。検証: マクド2支店合流61,650・未分類・降順OK。**入力欄方式は不採用の設計判断を維持**(摩擦・境界・遡及の三重苦)
- v3.15 **第3カテゴリ「承認」**(名称はユーザー案): EXPENSE_CATS一箇所定義に追加で両フォーム自動反映、チップ配色=blue(消費red/娯楽amber/収入tealと区別)。**予算バーなし=承認プロセス自体が統制**という設計思想(リュック等の奥様承認済み支出を娯楽警報機から隔離)。ほしい物リスト→承認→購入→承認計上のガバナンス動線。承認の内訳ドリルダウンは未接続(予算行が入口のため・需要が出たら)。**予算カードのループもEXPENSE_CATS共用だったためfilterで承認を除外**(一箇所定義の配管は両刃——カテゴリ追加時は予算ループへの波及を必ず確認)。**給料日起算は不採用で確定**(予定込み収支が既に解決・月キーは全系の土台・外界は暦月)。**ジャンル分析は「店→ジャンル辞書」のレンズ方式で設計合意待ち**(入力欄追加は摩擦・境界・遡及の三重苦で非推奨、チェーン集約と同哲学、内訳トグル第3モード案・1-2時間)
- v3.14 **分身事件・完全解決**: 診断ログが真相を確定——水道の¥7,443はID末尾-f2026-07-27=**27日生まれ**(テンプレは元・毎月27日)。ユーザーが実績を15日/¥7,443に編集後、**テンプレ自体を2ヶ月ごと15日へ変更**→updateFixed再生成が「07-15の発生」を探すも実績の出生日は07-27で別人判定→¥8,000(fD付き=v3.09以降生まれの唯一の刻印、時期も一致)を新規生成。**dedupeのスルーは設計通り(出生日が違う=同じ発生ではない)**。恒久対策=updateFixedに**養子縁組**: 再生成前、新スケジュールの発生日と同日付の自テンプレ実績にfixedDateを刻み直して代表認知(再現テストで双子ゼロ・¥7,443が07-15代表を確認)。残作業: ユーザーが既存の¥8,000をスキップ削除(1回)。ガス無事=スケジュール未変更、完全メシ双子=同一出生日だったのでv3.11掃除機が回収済み。**全事象矛盾ゼロで説明完了**
- v3.13 **トグル事件解決**: 真犯人=load()→applyData()のフィールドホワイトリストにopenAddOnLaunchが無く**毎起動で選別落ち**(保存は毎回成功していた)。ホワイトリストに追加で完治。**教訓: S直下に新フラグを足したらapplyDataのホワイトリストにも必ず追加**。migrateEntryは無罪(Object.assign保持)、全行(推定)は年内分が一括生成済みでv3.09以降の新規生成が無いだけの正常。診断強化: 全文クリップボードコピー(alertは700字で切る)+**迷子検出(fixedIdの参照先テンプレ不在)**——水道¥7,443の正体特定用。捜査残: 水道の2件がdedupe対象外の理由(迷子説が最有力)
- v3.12 捜査線の更新: **二重テンプレ説は棄却**(固定費一覧で各1行を実機確認)。トグルOFF巻き戻り=再現性あり→**新最有力仮説: iOSの即killでlocalStorage書込み消失**(テスト動作のON→即kill→起動が地雷パターン)。対策①pagehide/visibilitychange(hidden)で駄目押し保存 ②**固定費画面に「整合性チェック」診断ボタン(一時設置)**——fixedIdごとに全エントリのdate/金額/出生日(fD有無)/id末尾を列挙+戸籍喪失(元定期でfixedId無し)の検出。水道¥8,000がdedupe対象外=グループ不一致の内訳をこれで確定させる。検証手順をユーザーに依頼中: トグルON→ホームへ戻り5秒待機→kill→起動
- v3.11 **分身の自動掃除(fixedDedupe)**: 同一(fixedId, fixedOccKey)グループに複数エントリがいたら**テンプレ通りの未編集クローンのみ削除**(date===出生日 && amount===tpl.amount && store===tpl.store)、ユーザーが触った方は必ず残す・全員触っていたら全員残す。ensureFixedEntries冒頭+クラウドpull適用後に実行、削除時はトースト報告。検証4/4(実2ケース+誤爆防止2)。**背景: v3.09後も水道(金額のみ編集)・完全メシ(日付移動)で分身が再発。出荷コードのハーネス再現では分身ゼロ=生成ロジック無罪が確定**、真犯人は未特定(容疑: Safari別ストレージや旧SWキャッシュ端末=第二サイロがstale状態で生成→同期注入 / 二重テンプレ)。免疫方式でどの経路でも自動治癒する構えに転換
- v3.10 **起動時記帳(実験・トグル式)**: S.openAddOnLaunch(既定OFF)、ツール家計簿セクションのカードでON/OFF。ONならコールドスタート時にentryForm初期化→記帳画面直行(復帰時は元画面のまま=正しい挙動)。背景: iOSはPWAへの外部ディープリンク経路が皆無(URL起動=Safari別ストレージ/ショートカットAppを開く=引数不可)のため「全起動を記帳にする」逆転案。ウィジェットが閲覧を吸収した今なら成立するトレードオフ。微妙なら戻す前提の実験
- v3.09 **二重計上バグ修正(定期エントリの身元問題)**: 生成分の日付を編集すると照合キー(fixedId+date)が壊れ再生成で分身する報告(ガス料金27日→20日編集で実証)。対策=**fixedOccKey(e)**: fixedDate(新規生成時に刻む)→ID埋め込みの-fYYYY-MM-DD復元(**既存の移動済みエントリも遡及治癒**)→dateフォールバックの三段。生成器の照合・スキップ記録・次回計算すべて出生日基準に統一。5ケース検証済み。既に生まれた分身は手動削除(スキップ確認でOK)が必要
- v3.08 スキップ確認の行割り修正: confirmはOS描画で幅制御不可→**各行を折返し幅(約18全角)未満に意図分割**「今回分(MM/DD)をスキップします。/(定期設定は残ります)/次回は MM/DD に自動計上されます。/よろしいですか?」。最終分は「以降の予定はありません(最終分)。」に圧縮。**ネイティブダイアログも改行規律の対象**
- v3.07 スキップ確認文言の周波数対応: 月次時代の「来月以降は自動計上」を廃し、**fixedOccurrencesから次回発生日を実計算して「次回は MM/DD に自動計上されます」**(最終分なら「以降の予定はありません」)。skips適用後の探索・日付明示付き。ユーザーが祝日スキップ時に発見した化石文言
- v3.06 **固定費エンジン拡張(新機能は作らない判断)**: ①freq="days"(曜日指定)——days[]配列で月〜日の複数選択、開始日〜終了日の範囲で該当曜日に毎日発生(駐輪場・出勤日系のユースケース) ②**終了日tpl.end(全頻度共通・空欄=年内)**——fixedOccurrencesでyEndをmin(12/31,end)にキャップ、monthly系もdt<=yEndフィルタ追加。フォームは曜日チップ7個(月起点並び)+終了日date入力(fx-end、DOMから直読み)。祝日シフトは日付系頻度のみに限定。updateFixedの「未来purge→再生成」構造のおかげで終了日短縮時の残骸も自動解決。検証: 7/16開始・月〜土・8/31終了で7月14回+8月26回=40回・日曜混入ゼロ
- v3.05 ＋ボタンを他タブと完全同化: TAB_ICONS.add(丸プラス線画・stroke1.7)+「追加」ラベル、専用CSS撤去で既定の.tabs buttonに(選択色なし=タブでなくアクション)。ユーザー承認済みの最終形
- v3.04 **浮遊FAB廃止→ナビバー中央の主動詞ボタンへ**: ＋を「タブ従属」から「アプリの主動詞=記録を追加」に再定義(遊技タブ=スロ/麻雀ドラフト・それ以外の全タブ=UI.tab="ledger"に切替えて記帳フォーム)。平置き42px琥珀円(.fabtab/.fabc、張り出し・影・浮遊なし=静音)。ついでに旧addの最終else地雷(資産/ツールで押すとスロドラフトが開く)を撤去。コンテンツ占有ゼロ化
- v3.03 **FABのナビバー埋没修正+魔法数根絶**: 原因はv2.94で保存行をnavbar内に追加→navbarが約18px成長したのにfab(78px)/toast(76px)/#app下pad(80px)の固定値が置き去り。対策は数値上書きでなく**render毎にnavbar.offsetHeightを--navhへ実測注入**し、fab=navh+18/toast=navh+16/#app下pad=navh+20で自動追随(max(…,58〜60px)床でナビ非表示画面も安定)。**ナビバーの背丈に依存する固定値を今後書かないこと**
- v3.02 内訳画面の完成: ①**チェーン集約(既定)⇄店舗別トグル**——chainKey=「最初の空白(半角/全角)より前」規則で辞書レス集約(空白なし店名は単独扱いの安全劣化、集約時は明細行に支店名を薄表示)。**ローデータは正確・集計は粗く、のジレンマを入力習慣(空白区切り)の活用で解決** ②**明細タップ→編集画面へ直結**(drilledit、UI.entryReturn="catdrill"でback/esave/edel全出口が内訳へ帰還。bindEntryの出口はスライス内置換で3箇所改修)
- v3.01 家計簿側の新機能2件: ①**カテゴリ内訳ドリルダウン(catdrill)**——予算行タップ(上限設定済み時のみ、data-a=catdrill)→店別降順リスト(store空は「店名未入力」バケット・収支src(slot/mahjong)は除外)→店行タップで明細をアコーディオン展開(UI.drillOpenStore)。予算変更(setbudget/prompt)は内訳画面ヘッダーへ移設、ホーム行の文言は「内訳を見る」。ルーティングはUI.screen="catdrill"+専用bindCatDrill(setbudgetハンドラはbindMainと重複定義——bind分離構造の制約) ②**遊技タブアイコンを等角投影サイコロに刷新**(妻レビュー起点: 平面5目→立体1・2・3面、六角形シルエット+内稜線+pip円)。エントリのstore/itemフィールドは既存(定期テンプレ由来)でスキーマ変更なし
- v3.00 **区画状態のエフェメラル化(完成宣言)**: render()冒頭のRENDER_PREV_SCREENトラッカーで「他画面→mjtable」遷移を検知し、折りたたみをライフサイクル既定へ毎回リセット(同一画面内の再描画では不発=トグル操作は安全)。**展開は一時的な覗き込み、離れたら既定に戻る**がユーザー確定仕様。v2.97の4箇所エントリ初期化は撤去し実装点をrender()に一元化(toTable尊重方針は撤回)。遷移マトリクス4パターン検証済み。**ユーザーの完成条件を満たした——麻雀スコア表はv3.00でfeature complete**
- v2.99 清算画面IAの最終整理: ①保存済みルールchips(+編集)を**ルール設定カード内の先頭へ移植**(h += '' + 継続トリックで後続文字列に無改変接続) ②家計簿・共有バンドを**「精算・共有」区画に統合**(feesOpenが共有まで包含・bandは区画内の小見出しとして存置)。**畳み状態の完成形=日時/▸ルール設定/▸プレイヤー・チップ/対局モード/▸精算・共有/保存行の6要素・1画面完結**
- v2.98 **まとめて確定**: 回戦確定時、それより前に未確定行があれば「N件まとめて確定しますか?」を一括提案(完全入力行のみ・未入力行は件数トーストで除外報告)。ロック機能導入前の行の救済+確定し忘れの回収。逐次確定の通常プレイではダイアログ自体が出ない。18回戦確定→18〜29回戦バー併合の動作自体は正常だった(閉じないのは導入前の1〜17回戦という診断)
- v2.97 **3区画折りたたみ完成形(ルール/チップ/ゲーム代)**: 共通secBar部品(▸/▾+畳み時は要約——ルール=レート要約、チップ=名前列挙、ゲーム代=総額or未入力)。**初期化は4つのエントリポイント(mjtnew/mjtopen×2系統)で明示実行**(mjtInProgress(t)=スコアあり or メンバー確定→3区画とも畳む・新規はルール/チップ開+ゲーム代畳む。v2.96のビュー内lazy初期化は経路依存で不発だった疑い→保険として残置)。toTable(対局→設定)は手動状態を尊重し再初期化しない。チップ買い足しは「開始枚数を増やす」運用で記録可(専用機能不要とユーザー確認)
- v2.96 **ルール設定カードの折りたたみ**: ヘッダーバー(▸/▾)タップで開閉、**畳んだ姿は1行要約バー**(3麻・持ち/返し・1点単価・チップ・精算単位を自動生成)。**初期値=ライフサイクル連動: スコアが1つでもある or メンバー確定済なら畳む**(UI.rulesOpen、null=未判定でviewMjTable内lazy初期化、backでnullリセット→次の表で再評価)。手動トグルはセッション内で優先。対象はルール設定のみ(チップ/ゲーム代は精算常用のため畳まない)——**畳む条件の設計はライフサイクル情報を初期値に使う、が原則**
- v2.95 清算画面のセッション情報を行文法の1カード2行に転換: 「日時 [date][time]〜[time]」(日付と時間の1行統合はユーザー案)+「ルール名 [flex input]」。旧様式(9pxラベル上置き+44px大箱×4)の最後の残党を掃討、data-mt(date/tstart/tend/rname)は不変。ナビバー統合はユーザー承認(清算画面は最下部スクロール要だが許容)
- v2.94 **バッジ全撤去→ナビバー統合が最終解**: 保存表示はホームの下部ナビ(#navbar=appの外の常設シェル)内・タブ直上の9px行「✓ 自動保存 HH:MM:SS ・ 通信不要」に一体化(スクロール不要・不滅・浮遊物ゼロ)。清算画面末尾にもsaveIndicatorHtml()常設、対局モードは既存線を維持、ホーム末尾の文字行は重複のため撤去。**save()→saveLocal()経由は確認済み(計測点は正しかった。v2.93の敗因は配置の算数と過渡性設計)**。**却下事項: 浮遊バッジ/過渡パルス——蒸し返さない**
- (v2.93 保存パルス✓バッジ: 実機で位置ズレ・知覚不能により即日撤回): 全画面左下に22px・opacity.32で常駐(body直付=再描画耐性、ensureSaveBadgeをrender()先頭で冪等呼出)、saveLocalのたびにteal点滅550ms、タップで時刻トースト。**設計判断: 即時保存アーキでは「最終保存時刻=最終入力時刻」で常時タイムスタンプは情報量ゼロ——時刻の常時表示ではなく機構の生存証明を常時化**(ユーザーの「アイコンなのか？」案を採用)。ホーム/対局の文字インジケーターは説明文の役割で存置
- v2.92 ①**保存インジケーターをアプリ全体機構へ昇格**: 心臓部saveLocal()がLAST_LOCAL_SAVEを刻み.savetsクラス要素を一括ライブ更新、saveIndicatorHtml()を家計簿ホーム末尾+対局モードに常設。「✓この端末に自動保存済み HH:MM:SS(通信不要・オフラインでも消えません)」——**保存不安はアプリ構造の信頼問題としてユーザーが明言、通信非依存を文言で断言する方針** ②総額カードを行文法(ラベル+右寄せ入力110px)に再構築、2カラム構造と大型入力箱を廃止(高さ合わせの小手先を捨てて共通文法に従わせるのが正解だった)。ホームの表示位置はviewMain末尾(FAB直前)——**viewMainはタブごとに早期returnを持つ多出口関数、末尾アンカーは要注意**
- v2.91 ①総額/目安の高さ問題の真因解決: **推測値での高さ合わせをやめ、入力と目安の両方に明示的同一高さ40pxを付与**(smクラスの実高≈34pxと41pxのズレが悪化の原因だった——高さは推測せず両側に明示、が教訓) ②**共有画像の上部にDynamic Island安全余白PADT=110pxを焼き込み**(iPhone写真アプリのフルスクリーンで上端が島に隠れるiOS仕様への防衛。全員iPhoneの卓では実質必須)
- v2.90 ①途中経過シートの直近テーブル拡大漏れ修正(pr-mid一般則15px) ②総額入力と目安の高さ揃え(41px) ③**自動保存インジケーター**(saveMjtがLAST_MJT_SAVEを記録し#savetsをライブ更新、対局モード下部に常設——「保存されてるか不安で画面から出られない」への回答) ④**清算シートも画像共有に統一・PDFボタン全廃**(mjtMidImage(t,full)がチップ/収支/場代/経費/調整/支払+注記+単価行まで描画。支払行の上に2px強調線。print系コードは紙需要が出た時のため残置)
- v2.89 行内×を廃止し**削除モード方式**へ: 追加/確定の行に「削除」トグル(未確定かつ席数超過時のみ表示)→モード中は各行末尾(🔒列)に高さ36pxの整列した削除チップ。削除1件で自動解除。**行内に高さ文法の違う小物を浮かせない**が原則。精算単位注記を「未選択＝端数切捨なし」に平易化
- v2.88 共有画像さらに増強(ユーザー方針「余白は資源・セル上限まで」): 履歴24px/合計28px/直近26px/名前22px/ラベル類20px/行高52・46。グラフは現状維持で確定
- v2.87 視覚規律: メンバー確定チップをヘッダーから「＋プレイヤーを追加」の隣へ移設(ヘッダーは純テキストで全列同高)。**説明文の改行を運任せにしない原則**——精算単位の注記を意図した2行に分割、飛び注記2種を1行長に圧縮(「調/整」等の中間割れ排除)
- v2.86 共有画像を**セル幅基準で最大化**(列幅144pxに対し最長内容は3-4文字→履歴21px/合計24px太字/直近22px/名前19px/行高48・42)。グラフ改修: **罫線を最小/0/最大の3本のみ**に削減(形を読む図の思想)・線3px・終点3.5px・名前14px太字。それでも見づらければ次の一手は「順位推移(1〜4着の段差)グラフ」への転換案——点数の大小情報と引き換えのため未着手・要相談
- v2.85 清算画面の整理: ①精算単位をルール設定カード末尾へ移設し**プリセットにも保存/適用**(roundPay) ②総額カードに入力合計/過不足を統合(旧下部カード廃止) ③**ダークUIのプラス=var(--teal)で統一**(対局モードのセル/合計・チップ増減。紙面は黒のまま) ④チップカード: ヘッダー中央揃え・×を15pxに拡大・**メンバー確定ロック(playersLocked=×非表示、追加は可)**・**個人別チップ確定ロック(chipLocked[ci]、確定時にオートフィルを実数化して書込み、行はopacity .75+タップ不可、🔓/🔒トグル)** ⑤**pdelの潜在バグ修正: pexp/chipLocked/feeReliefのsplice漏れ+feeRelief.byの再indexing**(削除者を指す負担は未選択化)
- v2.84 **共有画像(mjtMidImage)の全要素を約1.3倍に大型化**(年配の可読性対応: 履歴16px/行高34・集計の合計18px太字・グラフ目盛り/名前12px・タイトル28px)。**シートと画像は別レンダラー(CSS vs canvas手描き)のため完全同一のテイストは原理的に不可**——設計トークン(色/縞/罫線)は共有済み、寸法変更時は両方を同時更新すること(v2.83でシートだけ拡大して差が開いた反省)
- v2.83 対局フロー完成形: ①一覧カードの金額をmjtSettle(調整込み)に修正 ②キーパッドの途中経過ボタン撤去 ③**対局モードはダークに復帰**(sheet-lightはmjsheetのみ、#appの上padding除去はnopad-topクラスに分離——play/sheet共通) ④途中経過シート(pr-mid)の履歴15px/集計13.5px拡大 ⑤**ロック済み連続区間の折りたたみ**(「🔒 X〜Y回戦 確定済み」バー、UI.lockedOpenで全展開、たたむボタン、toPlayでリセット) ⑥**確定(ラス半含む)後は自動で途中経過シートへ遷移**(sheetFrom=mjplayで戻る=対局)——卓の儀式「確定→全員で確認→次戦」をアプリの動線に
- v2.82 **バグ修正: lockrow/feesplitハンドラが未定義のPを参照して例外死(確定ボタンが無反応)** → 各ブロックで const P=t.players を定義。**bindMjTableのスコープにPは無い(tのみ)——ハンドラ追加時の定番の罠**。精算単位を[50/100/500/1000円]に(1円=丸めなしと同義のため廃止、**選択中の再タップで解除**)。スコア用キーパッドの「完了」を廃止(確定ボタンが主役)、**同じセルの再タップで閉じる**ジェスチャーに置換(チップ入力用キーパッドの完了は存置)
- v2.81 精算モデル最終形: **場代行=生の実費・調整行=場代移転+丸めの合算(Σ調整=0の不変量)**。免除は「金額指定(m:"amt",v)」と全額の2種(旧fracは計算互換のみ残置)。**各人の経費(t.pexp[ci]=飲食など店への個別分)**を新設——店総額=Σ場代+Σ経費、支払=収支−場代−経費(+調整)、シートに経費行。**均等割ボタン**: (総額−Σ経費)÷人数を場代へ一括入力(端数は★自分に加算・confirm付き)。目安表示も経費除き基準に
- v2.80 ①「端数処理」→「**精算単位**(1円/100円/500円/1000円=何円単位で精算するか)」に改称 ②**場代の免除・減額(t.feeRelief[ci]={m:"frac"|"full",by:cj})**: 場代行の免除チップ(なし→端数免除→全額免除の巡回)+負担チップ(他者を巡回)。計算順=①場代移転→②支払算出→③精算単位丸め(トップ集約)。端数免除の単位=精算単位(なし時100円)。負担者未選択は未適用+注記。移転内訳はシート注記に自動明記。店総額は常に不変
- v2.79 **回戦の確定・ロック機構**: キーパッドに「この回戦を確定→次へ」(全員分スコア列挙の確認ダイアログ→未入力なら拒否→t.locked[ri]=true→末尾なら次行自動生成)と「ラス半で確定」(次行を作らず終了トースト)。ロック行はタップ無効・opacity .8・左端🔒(タップ→確認→解除)。行削除時はlockedも同期splice。誤タップ防止はロックが担うため、対局の基本フローは「入力→確定→次へ」
- v2.78 端数処理を「トップ集約」方式へ改訂+調整行のセル表示化
- v2.77 チップ表をゲーム代と同じ**カード様式**に統一(罫線テーブル廃止)。開始/終了は「見た目=入力箱・挙動=キーパッドセル」のdiv(選択中は琥珀輪郭chipSel)。清算画面の視覚言語はカードに一本化
- v2.76 **途中経過の画像共有(LINE等)**: mjtMidImage(t)がcanvasへ直接描画(タイトル/集計/直近/グラフ/履歴、2倍解像度PNG)→mjtShareMid()がnavigator.share({files})でOS共有シートへ(非対応環境はダウンロードにフォールバック)。**foreignObject方式はiOS WebKitで不安定なため不採用と決定済み——蒸し返さない**。グラフ計算はmjtChartData(t)に共通化(SVG/canvas両対応)、色はMJT_COLS。導線=途中経過シートのヘッダー右+対局モード下部。**実機確認済み・一発合格(2026-07-13)**
- v2.74-75 スコア表示は**fmtPt(整数なら小数点なし)**へ一括置換(対象: 紙面セル/合計/対局ヘッダー/キーパッド頭。グラフSVG座標とスロット系の%・時間は対象外)
- v2.72 グラフを図版カード(枠#999・角丸6px・白背景)で囲み境界を明示、タイトルを「スコア推移(チップ除く)」に簡素化(「累積」は視覚的に自明のため削除)
- **半解決: iOSはprint時の@page margin:0を無視しURL/日付フッターを強制描画する——CSSでは消せないと確定。現状の実用解はv2.66の精算シート画面のスクショ。完全自動のPNG生成(SVG→canvas→share)は必要になったら着手**
- v2.57 **誤振替の取り消し**: 振替行はfromDataに元記録の完全コピーを保持。振替行の×=「家計簿へ戻す」(確認後、draft._restoreに積み、**保存時に**元記録を復元。保存せず戻れば何も起きない往復対称トランザクション。二重復元はidガードで防止。_restoreはセッションに永続化しない)。v2.54-2.56期の振替行はfromData無しのため×=通常削除
- Supabase: RLS検証、履歴テーブル+トリガー導入、RESTORE_RUNBOOK.md作成

## 8. 未対応・保留(ユーザー合意済み)
- 時給/分散ダッシュボード・月次トレンド: §0のトリガー到達後。
- 投資機能: 開始後(新年目安)。資産管理→ポートフォリオ評価・投資計画修正の下地となる記録。
- **ホーム画面追加時の名前候補が「台帳」になる問題**: メタ/manifest/titleは全て"Tally"、manifest参照は?v=2でキャッシュバスト済み。次の再追加機会に確認(**再追加はlocalStorage全消去なので名前確認のためだけにやらない**)。それでも出るならiOSのサイト別記憶で制御不能→一度手入力すれば以後記憶される見込み。
- ユーザー却下済み(蒸し返さない): 予算アラート/プッシュ通知、カテゴリ細分化、塗り潰しピル、ローカルスナップ世代数の増加(深い履歴はサーバの役割)、iOS 62ptへの追加CSS実験、貯金UIの投資画面への移設(多目的口座のため。AI判断への貯金組み込みはv3.29で監査資料側にて解決済み)、**アプリアイコンの変更(2026-08-01に8案を検証し全て却下→現行据え置きで確定)**。検証した案と敗因: 筆致/かすれ=整列という強みを壊し実寸で視認性が低下(輪郭の多角形近似では筆に見えない・要スタンプ方式)、陰影+階調/筆勢テーパー=実寸で変化が知覚できず再インストールのコストに見合わない、交差部の透け/斜線teal化=足した情報が縮小で消える。**結論: アイコンは60〜120pxで戦うため削ぎ落とした図形が強く、現行の均質さは単調さであると同時に安定感そのもの**。色替えの原則も確定: 白棒は「まだ意味の付いていない=家計簿全体」を担うため色を与えない(赤=消費/琥珀=娯楽/teal=収入/青=承認と衝突する)

## 9. 現在のバージョンとファイル
**v4.43**。リポジトリ構成: index.html / sw.js(v1.50・レビュー済み: 全相対パス、本体=ネットワーク優先+失敗時キャッシュ、静的物=キャッシュ優先) / manifest.webmanifest(レビュー済み: start_url・scope相対でrename安全、name/short_name:Tally) / apple-touch-icon.png・icon-192.png・icon-512.png(現行タリーマークデザイン) / HANDOFF.md(本書) / README.md(Tally版) / RESTORE RUNBOOK.md / **tests/(v3.97新設・node単体で動く検査ハーネス。使い方はtests/TESTS.md)**。**ウィジェットはリポジトリ外**(iCloud Drive/Scriptable/**Tally.js**)。作業が必要な時はユーザーに現物を貼ってもらうこと(手元に無い状態でブラインド編集しない)。**出力ファイル名は必ず`Tally.js`**——Scriptable側の実ファイル名と一致させることで、ユーザーのショートカットがリネーム無しで上書き保存できる(名前がズレると別スクリプトとして増える)。**デプロイ用ショートカット(iOS)**: `Deploy Tally`(index.html)/`Deploy HANDOFF`(HANDOFF.md)はWorking Copyへ Write→Commit→Push。**`Deploy Widget`はgitを使わず「ファイルを保存」でiCloud Drive/Scriptableへ直接上書き**(保存先=Scriptable、サブパス=`/Tally.js`、上書き=オン)。**保存アクションの入力は必ず「ショートカットの入力」にすること**——プレースホルダの`ファイル`のままだと何も起きず、しかもエラーも出ないので気付けない(実際にハマった)。
  **3つとも先頭にファイル名ガードを入れてある(v4.18)**: `名前を取得` → `もし [名前] が ◯◯ と等しい場合` の中でだけ書き込む。通す名前は Widget=`Tally.js` / Tally=`index.html` / HANDOFF=`HANDOFF.md`。**「等しい→書く」の向きにするのが要点**——「ではない→停止」だと判定が効かなかった時に書き込みが走るが、この向きなら判定が壊れても何も起きない。**誤タップでウィジェットのショートカットにindex.htmlを渡して即上書きされた事故が実際にあった**(Widgetはgitを通らないので復旧できない)。逆向き(Tally.jsをDeploy Tallyへ)はPWAが起動しなくなるのでさらに危険。
  **2026-07-24 リポジトリ名をkakeibo→Tallyへ改名(大文字T)**(Pages URL変更に伴い旧PWAエクスポート→新URL再追加→インポートで移行)。**localStorageキー"kakeibo:v1"は意図的に不変更**——変更=全データ孤立、URL無関係(index.html該当行にガードコメント有)。
sw.jsは一度も精読していないので、更新配信やキャッシュで不可解な挙動が出たらまずsw.jsをアップロードしてもらいレビューすること。
